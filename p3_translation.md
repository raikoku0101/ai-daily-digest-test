## 1. Introduction（はじめに）
検証可能な報酬を用いた強化学習（RLVR: Reinforcement Learning with Verifiable Rewards）は LLM の推論能力向上に有効だが、既存手法には計算コスト問題がある。PPO はポリシーモデルと同規模のクリティックが必要であり、GRPO は安定したグループ平均を維持するために1プロンプトあたり複数ロールアウトを要求する。本研究はこれらの課題をポリシーモデルの内部状態活用で解決する。

## 2. Method（手法）
POISE では、ポリシーモデルの順伝播（forward pass）で既に計算済みの隠れ状態（hidden states）とトークンエントロピー統計を入力とする軽量プローブを導入する。このプローブはプロンプトと生成トラジェクトリから期待検証可能報酬を予測し、ポリシーと並行してオンラインで訓練される。勾配の不偏性（gradient unbiasedness）を保証するため「クロスロールアウト構成（cross-rollout construction）」を採用し、あるロールアウトの価値を独立した別ロールアウトの内部状態から予測する設計にしている。

## 3. Experiments & Results（実験と結果）
Qwen3-4B および DeepSeek-R1-Distill-Qwen-1.5B を用いた数学推論ベンチマークで評価。POISE は DAPO と同等の性能を示しながら計算量を削減する。提案の価値推定器は LLM スケールの独立した価値モデルと同等の性能を達成し、単一ロールアウトのためプロンプト多様性が向上してゼロアドバンテージプロンプト検出コストも排除できる。

## 4. Conclusion（結論）
モデル自身の内部表現を活用することで、POISE は追加計算ほぼゼロで安定かつ効率的なポリシー最適化を実現する。勾配分散の低減と計算オーバーヘッドの排除により、大規模 LLM の強化学習に実用的な改善をもたらす。
