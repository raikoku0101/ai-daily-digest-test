## 1. Introduction (はじめに)
LLMが生成するコードパッチに「削除回避 (deletion avoidance)」という系統的傾向が存在する。開発者が削除すべきコードをモデルが保持し、代わりに条件分岐を追加する「Guard-and-Go」戦略を採用しがち。SWE-bench Verifiedの解決済みパッチでも、開発者の削除の28.3〜34.8%が残置されており、テストスイートが削除を検証しないため実際のマージ可能性が過大評価されている。

## 2. Related Work (関連研究)
人間は加法的変更を好む認知バイアス(additive bias)を持ち、これが言語モデルにも継承されている。SWE-benchを含む既存のコード編集ベンチマークは削除と追加の混在タスクをテストするため、削除能力を単独で評価できない。テスト合格がコード仕様の完全な実装を保証しないという既知の評価ギャップがある。

## 3. Method (手法)
**削除回避の測定**: 5つの主流モデル(GPT-5, Opus-4.5等)のSWE-bench Verified提出物を分析。開発者パッチとモデルパッチを比較して「削除リコール(deletion recall)」を計算。Guard-and-Go戦略の分類にはLLMベースのクラシファイアを使用。

**削除感応テスト構築**: 69の削除集約的タスク(削除が変更の25%以上)から、AST認識手法で重要な削除対象を特定。34タスクについて対象コードが残存すると失敗するテストを追加。

**CanItDeleteベンチマーク**: 200個のタスクを「削除のみ」で構成。occurrence-aware評価器と4段階の診断ラダー(vanilla→明示的削除指示→領域ポインタ→正確なスパン)で段階的に能力測定。

**削除焦点化post-training**: 7Bモデルのコードpost-training混合に12,821個の削除例(0.7%のトークン)を追加。

## 4. Results (結果)
**実践における削除回避**: 削除リコールは65.2〜71.7%。ファイル修正率92.5〜94.4%に対し、正確な行削除は44.6〜51.6%に低下。Guard-and-Goはpassingパッチの29.0%を占める。

**削除感応テスト効果**: 元テストで63.2%合格→削除チェック追加後41.9%に低下。21.3pp絶対低下。

**CanItDeleteでの性能**: Opus 4.8が79.0%で最高。完全削除失敗が主要な失敗メカニズム(69.8%)。

**Post-training介入**: CanItDelete成功が6.5%→13.7%に向上。SWE-bench Verified 5.3pp改善。

## 5. Conclusion (結論)
削除回避は過少学習(undertrained)であり、モデルの能力外ではない。少量の削除例を後訓練に加えるだけで系統的バイアスを緩和でき、コード編集全般の性能も向上する。
