## 1. Introduction (はじめに)

視覚言語行動（VLA: Vision-Language-Action）モデルの一般化能力は、利用可能なロボット実演データの規模と多様性に制約されています。ロボット実演の収集は高額で労働集約的ですが、エゴセントリック人間操作ビデオは大規模かつ多様に収集可能です。本研究では、人間の手の動きをロボット運動学に再ターゲット化し、人間腕を視覚的に置換するアプローチにより、エゴセントリックビデオから「18,561 時間のロボット学習データ」を生成します。この大規模合成データが、分布外一般化（out-of-distribution generalization）の向上に寄与するかを体系的に検証します。

## 2. Related Work (関連研究)

ロボット学習の規模拡大には、大規模実演データセット（Open X-Embodiment、DROID、AgibotWorld 等）と、人間ビデオからの学習アプローチが重要です。既存手法は視覚特徴抽出や報酬学習に焦点を当てますが、「人間ビデオの直接変換アプローチは小規模でのみ検証」されています。既存ベンチマーク（RoboTwin、LIBERO、CALVIN）は複数の分布シフトを束ねて評価するため、個別因子の分析が困難でした。

## 3. Method (手法)

パイプラインは 3 段階で構成されます。**アクション調整 (Action Retargeting)**: 21 個の手キーポイントから仮想グリッパー表現を抽出し、Savitzky-Golay フィルタリングで平滑化します。**視覚調整 (Visual Synthesis)**: SAM3 で腕領域をセグメント化し、ProPainter で背景再構成、グリッド探索と IK 解析でロボット基盤位置を最適化し、深度認識合成で 15 種類のロボット形態ごとにレンダリングします。**品質キュレーション**: パイプライン内フラグ・統計フィルタ・VLM 一貫性チェックの 3 段階フィルタリングを適用。カメラ座標系相対 EEF（End-Effector Frame）表現により、異なるカメラ設定とロボット具身化間で行動空間を統一します。

## 4. Evaluation Framework (評価フレームワーク)

RoboTwin2.0 を拡張し、4 つの独立した一般化軸を導入しました。**視覚外観**は背景・照明・ロボット色を個別制御、**シーン配置**は机高さ・クラッタ・カメラオフセット、**具身化 (Embodiment)**は 3 種類ロボット間のゼロショット転移、**タスク意味論**は未見物体と言語パラフレーズを検証します。この分解により、一般化向上の要因を精密に帰属できます。

## 5. Experiments (実験)

Ego2R + Robot (1:1) 混合比で、RoboTwin ランダム化で 53.5% 達成（ロボットのみ 50.9% 対比 +2.6）。視覚因子で顕著な向上を確認（背景 +4、照明 +8、ロボット色 +6）。具身化転移では ARX が 44→51 に改善。タスク意味論では未見物体で +11。EBench ではビューポイント比較で 3:1 混合が最良（+12.1）。実ロボット実験では全 5 タスクで向上し、特にブロック配置で +14 を達成。

## 6. Conclusion (結論)

大規模エゴセントリック操作ビデオから「15 具身化対応ロボット学習データを自動生成」するパイプラインを提案しました。分解された評価プロトコルにより、合成データは特に視覚・具身化・意味論的摂動下で相補的価値を提供することを実証。実ロボット展開で少数実演との組み合わせが有効であることも検証されました。今後は並列顎グリッパー以外の手形態への拡張と、生成モデルを活用したレンダリング高度化が課題です。
