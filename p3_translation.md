## 1. Introduction (はじめに)
閉じた状態のみから複数部品を持つ関節物体(家具など)の3D形状とキネマティック構造を復元する新しい問題を提案。従来の手法は「複数の異なる開き状態の観察」を必要としていたが、実世界では閉じた配置での観測が一般的。Vision-Language Model(VLM)・セグメンテーション模型・ビデオ拡散模型の出力を明示的なメッシュを介して相互検証するRest2Artを提案する。

## 2. Related Work (関連研究)
関節物体復元の既存手法は、カテゴリ固有の先験知識に依存するアプローチから、シーン最適化やファンデーションモデル活用へと進化。部品セグメンテーションでは、マルチビュー投票・Webスケール資産学習・対比学習が提案されている。本研究は「セグメンテーション模型とVLMの反復的相互検証」と「メッシュベースの幾何学的制約」という独自のアプローチで差別化する。

## 3. Method (手法) - 部品セグメンテーション段階
VLMが予測した部品階層木(part hierarchy tree)とSAM3の2D検出マスクを比較し、不一致を「修正信号」として利用する反復的な共精密化ループ(co-refinement loop)を実行する。検証済みのマスクは信頼度加重累積によってメッシュ上に投影され、グラフベースのラベル伝播で境界矛盾を解決する。

## 4. Method (手法) - 関節推定段階
ビデオ拡散模型（Wan2.2）で仮想的な開閉動作を合成し、CoTracker3で2D軌跡を抽出。動きタイプ（回転/並進）を分類後、再投影誤差最小化により剛体ジョイントパラメータを最適化。メッシュ幾何学を用いて「部品相互貫通(interpenetration)」を罰則化し、物理的妥当性を確保する。

## 5. Method (手法) - メッシュ補完段階
シェル状の部品パッチを、法線方向のオフセットで体積メッシュ(volumetric mesh)に変換。引き出しなどの内部構造が必要な部品には、パラメトリック内部形状を生成して統合する。これにより物理的に意味あるジョイント構造を持つデジタルツインが得られる。

## 6. Experiments (実験)
Articulated Containers Dataset(ACD)での評価で部品セグメンテーション精度で既存手法を上回り、ジョイント推定精度では「軸角度誤差11.35°、位置誤差0.85dm」を達成。MultiScan実世界データでも堅牢性を確認。共精密化ループなしの単純な組み合わせと比較して「階層木精度+10.2%」の改善が確認された。

## 7. Conclusion (結論)
Rest2Artは複数の不確実な模型出力を「メッシュ幾何学への接地(grounding)」を通じて統合する手法として、静止状態からの関節物体復元を実現した。メトリックスケール保持と物理的意味あるジョイント推定により、インタラクティブシミュレーションや空間計画などの応用に適したデジタルツイン構築が可能となる。
