## 1. Introduction (はじめに)

複数のタスク専門家モデルを単一モデルに統合するモデルマージ（Model Merging）は個別モデル運用の負担を軽減するが、統合後のモデルは専門家より性能が低下する問題がある。本論文は「特徴ドリフト（Feature Drift）」という観点からこの性能ギャップを分析。マージされたモデルと専門家が同一入力に対して生成する特徴の差異を追跡し、各層での局所的ミスマッチが後続層へどのように伝播するかを理論的に解明する。この知見に基づき、勾配降下やモジュール追加なしに閉形式解で層ごとに重みを調整するFeatCalを提案する。

## 2. Related Work (関連研究)

モデルマージ手法は主にパラメータ空間での直接的な重み統合（Task Arithmetic、TIES-Merging等）に基づく。一方、Surgery・ProbSurgeryといった後処理キャリブレーション手法はマージ後の特徴ドリフトを補正する補助モジュールを学習する。FeatCalはこれらと異なり、統合モデルの重みそのものを直接更新し、推論時に余分なモジュールを不要にする設計を採用。

## 3. Feature Drift Analysis (特徴ドリフトの分析)

層ごとの特徴ドリフトを「局所的ミスマッチ（Local Mismatch）」と「上流からの伝播（Upstream Propagation）」に厳密に分解。マージモデルの層関数と専門家の層関数が同一入力で異なる出力を生じる現象は、下層のドリフトが上層に影響を与える伝播項とともに最終層へ組み合わさる。この伝播メカニズムは微分可能性を仮定した平均感度演算子により追跡でき、最終特徴ドリフトは全層の局所的ミスマッチの加重和で表現される。

## 4. FeatCal Method (手法)

FeatCalは前向き順序（Forward Order）で層を巡り、各層で次の最適化問題を閉形式で解く。基本目的関数：キャリブレーションセットで固定した入力特徴に対し、マージ重み周辺での正則化付き二次回帰を実行。特徴補間（混合係数α）で上流ドリフトの影響を緩和し、アンカー正則化（係数ρ）で基盤モデルの知識も活用。タスク間のスケール差に対応する安定化重みを導入することで、閉形式更新式が得られる。勾配降下不要・推論時追加モジュールなし。

## 5. Experiments (実験)

CLIP-ViT-B/32で8タスクTask Arithmeticマージに対し、FeatCalはSurgery/ProbSurgeryの77.0%/78.8%を上回る85.5%を達成。拡張ベンチマーク（14・20タスク）でも一貫した優位性を保つ。FLAN-T5テキスト生成ではGLUE8タスクで基本モデル+6.3%改善。MergeBench Llama-3.2/3.1では3B/8Bで各+2.0/+2.3ポイント向上。サンプル効率も優れ、256例ではSurgery比4.1倍高速（53秒対217秒）。

## 6. Conclusion (結論)

特徴ドリフト理論により局所的ミスマッチと伝播の仕組みが明らかになった。FeatCalはこの洞察を活用し、前向き順序の層ごと更新と閉形式解を組み合わせることで、推論時オーバーヘッドなく効率的にマージモデルを改善する実用的ソリューションを実現した。
