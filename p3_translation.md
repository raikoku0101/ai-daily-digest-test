## 1. Abstract（概要）

MetaViewは、単一カメラ映像からの新規視点合成（monocular novel view synthesis）を実現する拡散モデルベースのフレームワークです。「暗黙的幾何モデリング（implicit geometry modeling）と最小限だが本質的な明示的3Dキューの組み合わせ」を特徴とし、大きな視点変化下でも幾何的整合性と精密な制御性を達成します。ECCV 2026に採録された高品質研究です。

## 2. Introduction（はじめに）

単一カメラからの新規視点合成は3D理解の核心課題ですが、大きな視点変化に対して幾何的整合性を保つことが困難でした。既存の拡散モデルアプローチは自由な画像生成には優れていますが、正確な3D制御性が不足していました。MetaViewは暗黙的・明示的3D情報の融合でこのギャップを解決します。

## 3. Method（手法）

feed-forwardジオメトリ知覚ネットワーク（feed-forward geometry perception network）から暗黙的幾何事前分布（implicit geometry priors）を抽出し、メトリック深度（metric depth）と組み合わせます。スケール認識（scale-aware）な幾何情報を拡散モデルに注入することで、視点変化に対してロバストな新規視点合成を実現します。

## 4. Results & Impact（結果と意義）

ECCV 2026への採録が示す通り、既存手法と比較して幾何的整合性と視点制御精度の両面で優れた結果を達成しています。自動運転・AR/VR・ロボティクスにおける3D再構成や、単眼カメラのみを用いたシーン理解への実用応用が期待される重要な成果です。
