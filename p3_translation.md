## 1. はじめに (Introduction)
マルチモーダル大規模言語モデル(MLLM)は数百〜数千のビジュアルトークンを生成し推論コストを増大させている。従来の削減手法は手作業設計に依存。本研究は「LLMが自動的に効果的な削減ポリシーを設計できるか」という問いを提示。一般的なアルゴリズム知識を特定ドメインの制約集約問題に適用することが課題。

## 2. 手法 (Method)
トークン削減ドメイン固有言語（TPDSL）を導入。131個の再利用可能な原子(atoms)で構成。候補を完全な新プログラムではなく「強力なベースポリシーへの残差修正」として表現。4つの構成要素: ①予算制御(B): トークン数目標 ②トークンスコアリング(S): 品質信号の融合 ③選択制約(C): 基準保持と交換割当量 ④再組立(R): トークン順序の復元。LLMがTPDSL状態空間を探索し評価器フィードバックで改良を繰り返す。

## 3. 実験設定 (Experiments)
14個のマルチモーダルベンチマーク（VQAv2、GQA、MME等）と3つのMLLMバックボーン（LLaVA-1.5-7B、LLaVA-NeXT-7B、Qwen2.5-VL-7B）で評価。94.4%のトークン削減下で99%以上の性能保持を目標に設定。

## 4. 主な結果 (Results)
既存手法（CDPruner、PruMerge+）と比較してLLaVA-1.5-7Bで3.2ポイント、LLaVA-NeXT-7Bで2.4ポイント上回る集計スコアを実現。FLOPs 9.9倍・プリフィル遅延 6.4倍削減。異なるLLMプロポーザーやベースポリシー間でも安定性を示し、追加探索なしの転移にも対応。

## 5. 結論 (Conclusion)
構造化残差探索によりLLM駆動のビジュアルトークン設計が実用的になることを実証。TTPDSLは実行可能性と予算遵守を保証し、基本方針の信頼できる構造を保存しながら不確実な選択のみを修正。今後の課題は豊かな探索空間表現と適応的評価戦略の探索。
