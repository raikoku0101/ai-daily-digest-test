## 1. Introduction (はじめに)

現代的なLLM開発において、事前学習（pre-training）と後学習（post-training）の間の中間学習（mid-training）段階が重要化している。中間学習は大規模で異質なデータソース（コード、数学、推論トレース、ツール使用ログ等）から構成されており、既存のデータ選択手法では対応困難。事前学習向けの手法は拡張性に優れるが意味的基準が曖昧で、後学習向けの手法は強力だが固定的なルーブリック（rubric）を前提とする。本研究ではMIRA（Mid-training Rubric Anchoring for Source-Aware Data Selection）を提案する。

## 2. Related Work (関連研究)

データ選択手法は3カテゴリーに分類される。(1)ヒューリスティック手法：perplexityやドメイン分類などの軽量フィルタを使用。(2)最適化ベースの手法：勾配情報やinfluence estimatesなどの暗黙的信号を活用。(3)意味的ルーブリックベースの手法：人間定義基準またはLLM判定による明示的な質的監督を提供するが固定的基準を想定。MIRAは群特定的アンカールーブリック（source-specific anchor rubric）を発見段階で導出し、その後学習可能なスコアラーに蒸留することで既存手法の長所を融合。

## 3. Method (手法)

MIRAは4段階で構成される。**第1段階**：コンテンツ埋め込み類似度に基づいて関連ソースを群化し、能力的に一貫したグループを形成。**第2段階**：フロンティア教師（frontier teacher）がサンプル記録に対して自由形式の質的判定を行い、その判定を「アンカー次元（anchor dimension）」にクラスタリング。**第3段階**：発見されたアンカーを使用して教師が構造化ラベルを生成し、群特定的な学生スコアラーに蒸留。**第4段階**：ソース条件付き信頼性マスク（reliability mask）で不信頼な次元を抑制し、ソース対応的な保持閾値で最終フィルタリング。

## 4. Experiments (実験)

コード指向の中間学習設定で実験を実施。21のデータソースを5つの能力群に組織化し、Qwen2.5-Coder-14Bをベースモデルとして使用。中間学習は50Bトークン全コーパスまたは25Bトークン選択サブセットで実施。評価はコード生成、多言語コード、SQL、ソフトウェアエンジニアリングの9ベンチマークで実施。全手法で同じ生コーパスを使用し、フィルタリング後のトークン数を統一して公正な比較を実現。

## 5. Results (結果)

MIRAの群レベル変種（MIRA-Group）は25Bトークン予算でマクロ平均スコア64.20を達成し、ランダムサンプリング（63.23）やDataMan（63.01）を上回った。長さロバスト性の分析では、PPLやDSIRなどの基準手法がシーケンス長に依存した偏りを示す一方、MIRAは長い記録（エージェント軌跡）にも対応可能。信頼性マスク分析では、各群の学生スコアラー信頼性が不均一かつ次元特定的であることが確認され、ソース条件付けの必要性が実証された。

## 6. Conclusion (結論)

MIRAは異質な中間学習データのソース対応的な意味的品質評価という観点からデータ選択問題を再構成。固定的なグローバルスコアに頼らず、能力指向のソース群に対して動的にルーブリックを誘導し拡張可能なスコアラーに蒸留。全トークン数の半分を使用しながら完全コーパス実行にほぼ匹敵する性能を達成。
