## 1. Introduction (はじめに)
オンポリシー自己蒸留（On-Policy Self-Distillation; OPSD）では教師と学生が同じ情報を受け取るため、有効な学習信号を提供する非対称性の構築が課題。既存手法は外部教師、特権情報（正解・ヒント）、視覚的証拠（トリミング画像）で非対称性を創出。本研究は「入力条件付けのみで非対称性を構築できるか」を問い、元画像と内容消去制御画像の予測分布の対比から蒸留ターゲットを生成する革新的アプローチを提案する。

## 2. Related Work (関連研究)
自己蒸留における非対称性構築の先行研究（特権情報活用、領域クロップ、報酬ベース手法）を概観。VCSDは「同一モデルの異なる視覚条件下での比較」という独自戦略を採用し、推論時の追加計算コストを回避する。

## 3. Method (手法)
三段階で機能する。(1) **制御条件**：黒画像を内容消去入力として使用。(2) **対比評価**：学生生成プリフィックスごとに教師が元画像・制御画像両方で次トークン分布を評価。(3) **形状調整**：対数確率差 Δₜ(v) = log p(v|元画像) − log p(v|制御画像) を計算し、妥当性制限（β値による相対閾値）内で調整。元画像分布が候補の妥当性を定義し、対比が視覚内容への依存度で確率を調整するという相補的役割分担が特徴。EMA 教師（φ ← μφ + (1−μ)θ）で段階的更新。

## 4. Experiments (実験)
Qwen3-VL 2B〜8B および Qwen3.5 2B〜9B で評価。BLINK・MMStar・MathVista・V*Bench・HRBench 等 7 ベンチマークを使用。Qwen3-VL-2B で +4.77 ポイントの改善。全スケールでオンポリシー蒸留ベースラインを上回り、言語漂流（Language Drift）抑制と訓練安定性も向上。

## 5. Conclusion (結論)
外部教師・特権情報・視覚的証拠なしで、入力条件付けのみから自己蒸留の非対称性を構築可能であることを実証。条件付き予測の変化が自己改善の有効な監督信号となる新たなパラダイムを確立した。
