## 1. Introduction (はじめに)

マルチモーダル大規模言語モデル (MLLM) は画像を数百から数千の視覚トークンに変換するため推論コストが増大している。視覚トークン刈り取り (Visual Token Pruning) はこの課題に対する有望なアプローチだが、既存手法は「手作業で設計されたヒューリスティック」に依存している。本論文はLLMを用いて効果的な視覚トークン削減アルゴリズムを自動設計するAutoPruneを提案する。

## 2. Related Work (関連研究)

従来の視覚トークン刈り取り手法は三カテゴリに分類される。重要度ベース手法は注視度やテキスト関連性に基づきトークンを選別、冗長性低減手法は視覚的に類似したトークンをマージ、多様性考慮手法は「情報カバレッジを最大化」する。LLM駆動アルゴリズム設計の分野ではLLMが反復的な提案と評価を通じてアルゴリズムを生成・最適化できることが示されているが、視覚トークン刈り取りへの直接適用は制約の複雑さから困難だった。

## 3. Method (手法)

**TPDSL探索状態表現**: 各候補を基本ポリシーπ_Pへの「残差修正 (Residual Modification)」として定義。状態α = (B, S, C, R)は予算制御 (Budget Control)、トークン評点 (Token Scoring)、選択制約 (Selection Constraint)、トークン再構成 (Token Reconstruction) で構成。131個の再利用可能なアトムが9機能グループに組織化される。

**LLM駆動ポリシー設計**: LLMがTPDSL構造化残差修正を反復探索。各ラウンドでLLMが候補を提案し、具体化・検証・評価を繰り返す。

**先行知識ガイド残差洗練**: 選択状態α*を基本ポリシーの有界残差洗練として適用。交換クォータqe=2が置換数を制限し、最小保持数rminを満たす設計。

## 4. Experiments & Results (実験と結果)

14個のマルチモーダルベンチマーク (VQAv2、GQA、MME等) と3つのMLLMバックボーン (LLaVA-1.5-7B、LLaVA-NeXT-7B、Qwen2.5-VL-7B) で評価。視覚トークン94.4%削減下でLLaVA-NeXT-7Bの完全トークン性能の99.9%を保持。FLOPsを9.9倍削減、プリフィル遅延を6.4倍削減。CDPrunerを最大3.2ポイント上回る。訓練不要で異なるアーキテクチャへの転移でも一貫した改善を示す。

## 5. Conclusion (結論)

AutoPruneはTPDSL領域特化言語により「制約認識残差修正」として探索空間を表現し、一般的なLLMの知識を特化的な刈り取り問題に適用可能にした革新的フレームワーク。複数ベンチマークとバックボーンにおける広範な実験で手作業設計手法を上回る効果と転移可能性が確認された。
