## 1. Introduction (はじめに)
大規模LLM事前学習ではパイプライン並列化（Pipeline Parallelism）が活用されるが、同期実装ではGPUがパイプラインバブルで遊休状態になり計算資源が無駄になる。非同期パイプライン並列化はバブルを排除して処理能力を最大化するが、「勾配の古さ（gradient staleness）」が課題とされてきた。

## 2. Method (手法)
PipeDream-2BWスケジュールを活用し、「パイプライン深度によらず一段階の勾配遅延に収まる」特性を利用。最適化器（optimizer）の選択が重要であることに着目し、従来のAdamWの代わりにMuonなどの新手法を採用。さらに誤差フィードバック（Error Feedback）に基づく汎用的な補正機構を導入し、勾配遅延の影響を軽減。

## 3. Experiments (実験)
最大10Bパラメータのモデルを用いた広範な評価を実施。非同期パイプライン並列化と同期学習を複数のモデルサイズ・設定で比較。最適化器の選択と誤差フィードバック補正の効果を系統的に検証した。

## 4. Results (結果)
提案手法により、非同期パイプライン並列化と同期学習の性能ギャップが解消されることを確認。「勾配遅延下での性能低下は最適化器の選択に強く依存する」という知見が大規模モデルでも成立。MuonはAdamWより遅延への耐性が高く、大規模非同期学習に適している。

## 5. Conclusion (結論)
一段階の勾配遅延は本質的な障壁ではなく、適切な最適化器選択と補正技術により克服可能。非同期パイプライン並列化の実用性が大幅に向上し、GPUバブルなしの高効率LLM事前学習が実現可能となる。大規模AI学習インフラのコスト削減に直接貢献する知見である。
