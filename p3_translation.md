## 1. はじめに (Introduction)
ユーザー定義セグメントマップに基づく大規模3D世界生成フレームワーク「Map2World」を提案。既存手法はグリッドレイアウト制限とオブジェクトスケール不一致の課題を抱えていたが、本手法は任意形状・スケールのセグメントマップに対応しグローバルスケール一貫性を保証。

## 2. 関連研究 (Related Work)
3D世界生成は(1)生成画像/動画からの3D再構成と(2)明示的3D表現の直接生成に大別。既存拡張手法はパッチベース/アウトペインティングで2D平面領域に限定されセマンティック制御が粗い。本研究は多窓拡散フレームワーク (multi-window diffusion) を3D体積領域に拡張する。

## 3. 手法 (Method)

### 3.1 構造化潜在空間での空間拡張
TRELLISの事前学習モデルを活用し、64×64×64の立方体窓を重複配置。各位置の速度場 (velocity field) をガウスカーネル重み付け融合で統合しシームレス接続を実現。セグメントマップ条件付けでは複数テキストプロンプトの速度を正規化ガウスカーネルで加重平均。スペクトル域パラメータ化 (spectral-domain parameterization) により安定した勾配最適化を実現。

### 3.2 3D潜在空間での詳細強化
詳細拡張ネットワーク (detail enhancement network) が大立方体から8小立方体への構造化潜在を推定。MLPレイヤーと流れTransformer (flow Transformer) を組み合わせグローバル構造情報を保持しながら詳細追加。トリリニア補間 (trilinear interpolation) による特徴統合と隣接立方体条件で境界シームレス接続を保証。

### 3.3 SLATデコーダの微調整
部分シーン立方体に対応するためデコーダをメッシュ再構成損失 (mesh reconstruction loss) で微調整し、完全オブジェクト学習との分布乖離を解消。

## 4. 実験 (Experiments)
NuiScene 43ラベルから35シーンの高品質メッシュを抽出し17,500立方体のトレーニングペアを構築。比較手法: SynCity, GaussianCube。

独自の World Quality (WQ) メトリクス: S(鮮鋭度)・W(完成度)・C(一貫性)・R(リアリズム)の4指標を設計。

## 5. 結果 (Results)
- WQスコア: Map2World 7.76 > SynCity 7.25 > GaussianCube 5.08
- CLIP-Scoreによる領域テキスト一貫性でも優位性確認
- スペクトル域パラメータ化により5ステップ以内で目標スケール到達
- 任意形状セグメントマップでグリッド制約なし大規模オブジェクト生成に成功
- 再帰的詳細拡張で4倍解像度への段階的アップスケール対応

## 6. 結論 (Conclusion)
TRELLIS事前学習知識を活用しテキスト指導型3D世界生成パイプラインを実現。セグメントマップ柔軟条件付け・詳細強化一貫性・ドメイン汎化により既存手法の制約を克服。スケーラブルで多目的な3D場面生成を可能にする。
