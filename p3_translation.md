## 1. Introduction (はじめに)

現在のLLMは閉形式コーディングタスクで優れた性能を発揮していますが、「最適解が存在しない・複数の正解が許容される」オープンエンド型では依然として性能が限定的です。根本原因はトレーニングデータの極端な不足であり、手作業での構築には膨大なコストがかかります。本研究ではこのデータ非対称性を解決するため、閉形式問題を自動的にオープンエンド化して大規模に合成する FrontierSmith を提案します。

## 2. Related Work (関連研究)

先行研究は主に三領域：閉形式タスク向け合成（AutoCode、SWE-smith等）、オープンエンド評価ベンチマーク（FrontierCS、ALE-bench等）、進化的アプローチ（WizardLM、FunSearch等）に分かれます。FrontierSmith は問題定義自体を変異させる点で既存手法と根本的に異なります。

## 3. Method (手法)

FrontierSmithのパイプラインは4段階で構成されます：

**段階1（変異生成）**: コンペティティブプログラミング問題を三軸で変異：目的の変更（決定問題→最適化問題）、出力制約の強化（多項式問題→NP困難化）、入力の一般化（特殊グラフ→任意グラフ）

**段階2（粗いフィルタリング）**: LLMジャッジで候補がオープンエンド条件を満たすか確認

**段階3（アイデア発散度フィルタ）**: 複数の独立した解を生成し、「異なるアルゴリズム戦略を使う確率」を定量化。LLMベースの比較とテスト実行による距離ベース推定の二段階を採用

**段階4（検証基盤構築）**: テストケース生成エージェントと検証器生成エージェントが相互検証しながら反復

## 4. Experiments (実験)

FrontierCS（172問）とALE-bench（40問）の二つのオープンエンド型ベンチマークで評価。Qwen3.5-9BをGRPO（グループ相対方針最適化）で200問の合成問題を用いて訓練した結果、FrontierCSで10.62（人間作成基準11.17に対し）、ALE-benchで633.58（ALE-bench直接訓練657.40に対し）を達成。同モデルを閉形式HardTests問題で訓練すると5.38・397.18にとどまり、オープンエンド化と連続スコアリング信号の両者が必須であることを実証。

## 5. Results & Analysis (結果と分析)

アイデア発散度メトリクスはオープンエンド型（~0.40）と閉形式（~0.14）を明確に分離する有効な分類器として機能。27Bモデルでも同様の傾向が確認され、提案手法のスケーラビリティを実証。フィルタ除去時は性能が2.05ポイント低下し、フィルタリングの重要性が確認されています。

## 6. Conclusion (結論)

FrontierSmithは変異とアイデア発散度フィルタリングにより閉形式問題をオープンエンド型へ自動変換するパイプライン。専門家による手作業なしにオープンエンド型コーディングの大規模トレーニングデータ合成の実現性を確立した。
