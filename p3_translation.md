## 1. Introduction (はじめに)
現代のAIエージェントは「エージェントハーネス（agent harness）」の中で動作する。従来のRL研究ではトレーナーが環境ループを制御していたが、「ハーネス付きエージェンティックRL（Harnessed Agentic RL）」ではデプロイ時のハーネスが制御を保持したまま、LLM API経由でトレーニングシステムがモデル呼び出しを観測・最適化する新パラダイムを提案。既存の複雑なハーネスを大幅に変更せずにRL訓練を統合できる。

## 2. Challenges (課題)
ハーネス付きRL実装には4つの根本的な課題がある。①再トークン化と標本マージ: テキストレベルで同一でも再トークン化後のトークンIDが異なりトークンレベルの連続性が破綻する。②報酬計算: 1つのロールアウトが複数の訓練標本に分裂し、報酬配分が不明確。③損失正規化: 動的な標本数により異なる勾配重み付けが生じる。④バックエンド複雑性: 固定GPU数に対しロールアウト後に標本数が判明するためスケジューリングが困難。

## 3. System Design (システム設計)
Agent Lightning v1.0は3コンポーネント構成。**APIゲートウェイ**は「ロールアウト（rollout）」という宣言的抽象化を中心に、ハーネスからのLLM呼び出しをモデルエンドポイントにルーティング。**ロールアウトコントローラ**はKubernetesジョブとしてエージェント実行を管理。**カスタマイズトレーナ**（VERLベース）はロールアウト終了後に訓練標本を構築。「コロケーテッド非同期RL」で同一GPUプール上でロールアウトと重み更新を時間シェアリングさせGPUリソースを削減。

## 4. Experiments (実験)
3つのエージェントタイプで検証。①検索エージェント（HotpotQA）: 報酬25.1%→41.7%。②一般指示追従エージェント: 51.9%→70.2%。③コーディングエージェント: SWE-smithデータセット6,000例でQwen3.5-9BのSWE-benchスコア41.8%→56.4%（+14.6%）。ロールアウトレベルの報酬計算と損失正規化の組み合わせが最良の結果を産出。

## 5. Related Work (関連研究)
従来のRL枠組み（verl、AReaL、slime）はエージェントループを直接実装する必要があり、エージェント統合が困難だった。Agent Lightning初版がLLMエンドポイント経由のプロキシ方式を導入。本研究はKubernetesによるセルフホスト実行を採用し約3,500行のコードで実装。

## 6. Conclusion (結論)
「ハーネス付きエージェンティックRL」パラダイムの課題を体系的に特徴付けAgent Lightning v1.0を提示。再現可能なデータ処理パイプラインと報酬ハッキング防止策を公開し、実践的なハーネス付きRL研究の基盤を提供する。
