## 1. Introduction (はじめに)
言語モデルの文脈長が128K・1M以上へ拡大する中、SDPA（Scaled Dot-Product Attention）のΘ(N²)計算量とメモリ消費が主要ボトルネック。提案するLighthouse AttentionはQ・K・Vを多層ピラミッドで圧縮し選択ロジックを注意カーネル外に配置、通常のFlashAttentionを圧縮部分列で実行。訓練後に密集注意へ復帰させると同トークン予算で密集ベースライン以上の性能と1.4〜1.7倍の壁時計短縮を実現。

## 2. Related Work (関連研究)
線形注意やSSMファミリーは状態圧縮で漸近的に高速だが長距離想起が限定的。ブロック単位剪定（MoBA・NSA）やトークン単位剪定（DeepSeek Sparse Attention・HISA）は推論時有効だがカスタムスパース核が必要。Lighthouseは訓練時の正確性を中核評価基準とし、密集SDPA復帰後にモデルの全注意能力が保全されることを検証。

## 3. Method (手法)
Lighthouse注意は4段階パイプライン：
(1) **ピラミッド構築**: Q・K・Vを対称平均プーリングしL層ピラミッドを生成。
(2) **スコアリングと選択**: パラメータフリーなℓ₂ノルムスコアリングで全ピラミッド要素から上位k個をchunked-bitonicカーネルで選出。
(3) **密集部分列注意**: 選択要素をS≪Nサイズの連続部分列に集約しFlashAttentionで計算。
(4) **散布戻し**: 出力をp^ℓ個の基本位置に分散。
上位k選択は非微分可能で勾配を流さず、WQ・WK・WVが「選択時に有用な値」を生成するよう学習される。

## 4. Complexity Analysis (複雑度分析)
L=log_p(N/k)とすると部分列サイズS=Θ(k log N)。注意段階の計算量はΘ(k² log² N · d)に削減。全体で層単位計算量はΘ(N·d)の線形スケーリングを達成。文脈並列化では各ランクが局所的にプーリング・スコアリング・選択を実行し密集部分列が標準リング注意で回転。

## 5. Experiments (実験)
SDPA復帰性テスト：98K文脈で530M Llamaモデルを16K最適化ステップ訓練（最初10〜12kステップはLighthouse、残りは密集SDPA）、復帰後損失（0.698〜0.701）は密集ベースライン（0.724）を下回り性能向上を確認。スケーリング則：512K文脈で前向き計算21倍・順逆17.3倍高速化を達成。設計アブレーション：最適設定はL=3・p=2・k=1536（損失0.6825）で全構成が密集ベースラインを上回る。

## 6. Conclusion (結論)
Lighthouse Attentionは対称Q/K/V圧縮と選択・注意分離によりΘ(N·d)線形スケーリングを実現。パラメータフリー設計で補助損失なく学習、B200で1.4〜1.7倍の壁時計短縮と1M文脈マルチノード訓練に対応。限界：対称プーリングは自己回帰デコード非対応で推論時は密集復帰に依存。
