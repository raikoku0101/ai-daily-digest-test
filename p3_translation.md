## 1. Introduction (はじめに)
大規模言語モデル（LLM）の推論速度改善において、投機的デコード（Speculative Decoding）は有力な手法である。小さなドラフトモデルが候補トークンを生成し、大きなターゲットモデルが一括検証することで並列処理を活用する。しかし、ドラフトモデルを大きくしても受理率（Acceptance Rate）が一定以上向上しない「スケーリング上限」問題が確認されており、本研究はこれを根本的に解決する。

## 2. Background (背景)
従来の投機的デコードはシーケンシャルなドラフト生成が主流。ドラフト長を増やしても受理率が伸び悩み、結果としてスループットの向上に限界があった。並列候補生成の試みも存在するが、因果的整合性（Causal Consistency）の確保が困難だった。

## 3. Method: JetSpec (手法: JetSpec)
JetSpec は「並列ツリードラフティング（Parallel Tree Drafting）」と「因果的コンディショニング（Causal Conditioning）」を組み合わせた新フレームワーク。ツリー構造で複数のドラフト候補を同時生成し、各ノードが前のトークン列を条件として持つことで因果的整合性を維持。ターゲットモデルはツリー全体をバッチ検証するため、1回のフォワードパスで多数の候補を処理可能。

## 4. Experiments (実験)
GSM8K、MATH などの数学推論ベンチマーク、HumanEval などのコード生成タスクで評価。ベースラインとして標準の投機的デコード、SequoIA、Medusa 等と比較。A100 GPU 上での実測スループット（トークン/秒）で評価。

## 5. Results (結果)
数学ベンチマークで最大 **9.64倍** のスループット向上を達成（標準の自己回帰デコードと比較）。従来の投機的デコードを大幅に上回る受理率を実現。ドラフトモデルのスケールアップに伴い受理率が継続的に向上することを確認（スケーリング上限の突破）。

## 6. Conclusion (結論)
JetSpec は並列ツリードラフティングにより投機的デコードのスケーリング限界を突破した。推論コストの大幅削減は LLM の実用展開を加速し、特に計算集約型タスクでの恩恵が大きい。今後の研究課題は、より多様なタスクドメインへの適用とメモリ効率の改善。
