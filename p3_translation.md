## 1. Introduction (はじめに)

マルチモーダル大規模言語モデル（MLLM）は数百〜数千個の視覚トークン（Visual Token）を生成するため推論コストが増大します。既存の剪定（Pruning）手法は注意度重要性・トークンマージ・冗長性推定など手作業設計のヒューリスティックに依存し、多様な目標・予算への対応が困難でした。本研究ではLLMが自動的に効果的な視覚トークン削減アルゴリズムを設計できるかに着目し、「AutoPrune」を提案します。

## 2. Related Work (関連研究)

視覚トークン剪定研究は三分類されます：(1)重要度ベース手法（FastV、PyramidDrop）、(2)冗長性削減手法（PruMerge、TRIM）、(3)多様性考慮手法（DART、CDPruner）。LLMを用いた自動設計はニューラルアーキテクチャサーチ（NAS）や数学的発見に応用されていますが、剪定のような制約密集型タスクへの直接応用には課題が残っていました。

## 3. Method (手法)

AutoPruneの核は**トークン剪定ドメイン固有言語（Token Pruning Domain-Specific Language: TPDSL）**です。131個の再利用可能な原子（Atoms）から構成され、予算制御（B）・トークン採点（S）・選択制約（C）・トークン再構成（R）を定義します。各候補を「強力な基礎ポリシーへの残差修正（Residual Refinement）」として表現することで探索空間を縮小し、LLMの注意力を性能に重要なポリシー要素に集中させます。

## 4. LLM-Driven Policy Design (LLM駆動ポリシー設計)

LLMは10ラウンド×5候補で反復的にTPDSL状態を提案。各候補は(1)具体化（Materialize）→(2)安全性検証（Safety Check）→(3)タスク評価のプロセスを経ます。検証失敗・低性能候補はフィードバックとして次ラウンドに組み込まれ、効果的なTPDSL構成の学習を促進します。

## 5. Experiments & Results (実験と結果)

14個のマルチモーダルベンチマークと3つのMLLMバックボーン（LLaVA-1.5-7B、LLaVA-NeXT-7B、Qwen2.5-VL-7B）で評価。94.4%のトークン削減下で99.7%の性能を維持。FLOPsを9.9倍、プリフィル遅延を6.4倍削減。CDPrunerと比べ3.2ポイント改善。異なるLLMプロポーザー間の最大差は3.30ポイントのみで、TPDSLの設計空間とフィードバックが支配的な役割を担います。

## 6. Conclusion (結論)

AutoPruneはLLMが制約認識的な残差修正としてアルゴリズムを設計できることを実証しました。訓練不要で、転送時に予算・アーキテクチャ依存フィールドのみ再具体化すればよく、柔軟な展開が可能です。AI自身がAIの効率化手法を設計する「AI4AI」アプローチの有効性を初めて示した研究です。
