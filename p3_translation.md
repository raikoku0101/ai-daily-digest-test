## 1. Introduction (はじめに)
Dense Retrieval（密集型検索）は情報検索で高性能を発揮するが、高次元埋め込みを通じた意思決定プロセスが不透明という課題がある。既存説明手法は字句一致・トークン整列などの表面的シグナルに依存し、埋め込みレベルの潜在因子への洞察が限定的。本論文はこの根本問題を解決するXetrievalフレームワークを提案する。

## 2. The Xetrieval Framework (フレームワーク)
2つの主要コンポーネントで構成。**推論内部化器（Reasoning Internalizer）**: LLM生成のChain-of-Thought（CoT）推論を埋め込み空間に直接近似する軽量MLP。Query Intent・潜在制約・証拠要件を含む推論指向情報を単一フォワードパスで注入し、高コストな自己回帰生成を回避。**機械的説明器（Mechanistic Explainer）**: Sparse Autoencoder（SAE）を採用し、推論強化埋め込みを疎（スパース）で人間解釈可能な特徴に分解。各特徴は自然言語説明と関連付けられる。

## 3. Related Work (関連研究)
既存アプローチはSPLADE・ColBERTなどの「固有の解釈可能アーキテクチャ」と帰属法・プローブなどの「ポストホック分析」に大別。Xetrievalは標準的なDense Retrieversを修正せず、埋め込みレベルで推論指向因子を直接説明する点で新規性がある。

## 4. Experiments (実験)
BRIGHT・NQ・MuTual等7ベンチマークで評価。推論内部化器はCoT生成比で計算効率を大幅改善しながら検索性能の75-85%を回復。TopK-SAE（k=256）が再構成誤差・単調意味性・検索保持性の最適バランスを実現。抽出特徴の操作でペアレベル類似度が最大変化し、メカニズムの有効性を確認。

## 5. Conclusion (結論)
Xetrievalは不透明な類似度スコアを超えた埋め込みレベルの説明を実現。推論内部化と疎特徴分解により検索決定を潜在的クエリ文書因子まで追跡可能に。多様なリトリーバー・ベンチマークで一貫した解釈可能な特徴を発見し、タスクレベルの特徴ステアリング（Feature Steering）も実証。将来は多モーダル・言語横断への拡張を予定。
