# 📅 Claude Code 週刊アップデート — Week 32 · August 3–7, 2026

> Claude Code のセッション同士がメッセージを送り合えるようになり、セルフホスト環境が自社インフラ上でクラウドセッションを実行できるようになりました。また、auto モードがデフォルトの権限モードになります。

## 🚀 主要機能 (3 件)

### Cross-session messaging `v2.1.224`
Claude Code のセッション同士がメッセージを送り合えるようになりました。Claude は `ListAgents` ツールで他のセッションを検出し、`SendMessage` で送信します。ユーザーが指示した場合だけでなく、あるセッションの変更が別のセッションの作業に影響する場合など自律的にメッセージを送ることもあります。メッセージはClaudeが相手セッション向けに書いたテキストであり、会話履歴やファイルは送信されません。macOS および Linux で利用可能。v2.1.224 以降が必要です。
🔗 https://code.claude.com/en/cross-session-messaging#message-another-session

### Self-hosted environments `v2.1.224`
セルフホスト環境により、Team・Enterprise プランでパブリックベータとして、自組織のインフラ上で Claude Code クラウドセッションを実行できるようになりました。`claude self-hosted-runner` をマシンまたはコンテナ上で実行することでランナーに変換できます。claude.ai、モバイル・デスクトップアプリ、または `claude --cloud` からセッションを開始する際にこの環境を選択すると、セッションは社内ネットワーク内で実行され、内部サービスにアクセスできます。Owner または管理者が先に管理者設定で「セルフホスト環境を許可」を有効化する必要があります。
🔗 https://code.claude.com/en/self-hosted-environments-quickstart#set-up-an-environment-and-runner

### Auto mode becomes the default `CLI`
8月14日より、Pro・Max・Team プランの新規セッションではデフォルトの権限モードが auto モードになります。自分でデフォルトモードを設定済みの場合は、一度表示される切り替えプロンプトを承認しない限り維持されます。組織が管理するデフォルトも変更されません。いつでもモードを切り替えることは可能です。既にこれらのプランで適用済みの変更として、auto モードが行う分類器の呼び出しが使用量制限にカウントされなくなっています。
🔗 https://code.claude.com/en/permission-modes#eliminate-prompts-with-auto-mode

## ✨ その他のアップデート

- VS Code 拡張機能に Focus ビューが追加。ターン毎のツールアクティビティを折りたたみ可能な1行に隠す機能。コマンドメニューまたは Ctrl+Alt+F (Mac では Ctrl+Option+F) で切り替え可能。
- サンドボックスの認証情報ファイルが Linux および WSL2 で `mode: "mask"` に対応。サンドボックス化されたコマンドがセンチネルコピーを読み取り、サンドボックスプロキシが外部送信時に実際の値に置換する。認証情報マスキングに `extract`、JWT 対応 `decode`、AWS SigV4 再署名オプションも追加。
- マーケットプレイスが新しい `archive` ソースでプラグインを zip アーカイブとして配布可能に。オプションの SHA-256 ピンを付けて HTTPS でダウンロードでき、git や npm なしでインストール可能。
- `/review` が `/code-review` のエイリアスになり、`/code-review` をエフォートレベルなしで実行すると前回指定したレベルが再利用される。
- `/fork` でコピーしたセッションが、元のセッションのチェックアウトではなく専用のワークツリーでコードを変更するようになった。
- `/plugin` からインストールしたプラグインが、安全な場合は現在のセッション内で即座にアクティブ化される。インストールサマリーに「Plugin is now active.」または `/reload-plugins` の実行を促すメッセージが表示される。
- ワークツリー内でコードを変更したバックグラウンドセッションが終了前にコミット・プッシュするようになった。タスクが必要とする場合のみドラフト PR を作成し、CLAUDE.md の git 指示に従う。
- セッションあたり 200 サブエージェント上限が撤廃。長時間実行セッションが新しいサブエージェントの作成を拒否しなくなった。同時実行数と深さの制限は引き続き適用される。
- リポジトリのチェックイン済み設定から Remote Control 自動接続を有効化できなくなった。`remoteControlAtStartup` はユーザー設定またはマネージド設定で設定する必要がある。プロジェクト設定とローカル設定では無効化のみ可能。
- ワークツリーの分離が強化され、ファイル編集だけでなくメインチェックアウトにアクセスする Bash コマンドと git リダイレクトもブロック。すべてのセッションタイプとサブエージェントに適用。
- Bash コマンドが権限チェックの一部を隠せなくなり、タブや不可視 Unicode パディングによる承認ダイアログでのコマンド偽装も防止された。
- PreToolUse 自動許可フックが、サマリーやコンパクションなど Claude Code の内部サイドタスクにおけるツール制限を回避できなくなった。
- Ultraplan リサーチプレビューが削除。`/ultraplan` コマンドと `ultraplan` キーワードを含む。プランモードまたは Web 版 Claude Code を使用すること。

📄 原文: https://code.claude.com/docs/en/whats-new/2026-w32
