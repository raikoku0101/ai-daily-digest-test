# AI Daily Digest — 2026-07-20

## Claude Code / Anthropic アップデート

### v2.1.215 (2025-07-19)
**主要機能**:
- `/verify` と `/code-review` スキルが自動実行されなくなりました。ユーザーが明示的に `/verify` または `/code-review` を呼び出した場合にのみ実行されます。

**ユーザーへの影響**: コードレビューや検証が意図せず実行されることがなくなり、ユーザーがより細かく実行タイミングをコントロールできるようになります。

---

### v2.1.214 (2025-07-18)
**バグ修正**:
- `dir/**` 形式の許可ルール (`Edit(src/**)` 等) が、指定ディレクトリ外のネストされたディレクトリへの書き込みを誤って自動承認していた問題を修正。
- Windows PowerShell 5.1 セッションで実行されるコマンドのパーミッションチェックバイパスを修正。
- ファイルディスクリプタのリダイレクト形式での Bash パーミッションチェック漏れを修正 (フェイルクローズに変更)。
- 10,000文字超の非常に長いコマンドが自動実行されてしまう問題を修正 (常にプロンプト表示するよう変更)。
- zsh の変数添字・修飾子が `[[ ]]` 比較内で不活性テキストとして扱われ、コマンドが承認なく実行されていた問題を修正。

**ユーザーへの影響**: セキュリティ面での複数の重要なバグが修正され、パーミッション管理がより安全になりました。

---

### v2.1.212 (2025-07-17)
**主要機能**:
- `/fork` が新しいバックグラウンドセッションに会話をコピーするようになりました (`claude agents` に独立した行として表示)。従来の `/fork` で起動していたサブエージェントは `/subtask` に変更。
- `claude auto-mode reset` コマンドを追加。デフォルトの自動モード設定を復元 (確認プロンプト付き、`--yes` でスキップ可)。
- WebSearch ツール呼び出しのセッション上限を追加 (デフォルト 200 回、`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` で設定可)。暴走した検索ループを防止。
- サブエージェント生成のセッション上限を追加 (デフォルト 200 個、`CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` で上書き可)。`/clear` で予算リセット。

**ユーザーへの影響**: 並列作業フローがより整理され、リソース使用の暴走を防ぐガードレールが追加されました。

