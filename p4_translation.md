## 1. Introduction (はじめに)

ペアデータなしで画像・動画編集モデルを訓練するフレームワーク「Bootstrap Your Generator (ByG)」を提案。従来の教師あり学習は数百万のペアサンプルが必要で、特に動画編集では収集が困難だった。提案手法は事前学習済み生成モデルの知識を活用し、サイクル一貫性（cycle consistency）で構造を保存することで、外部信号なしに訓練を実現。ユーザー研究で75%以上の選好率を達成し、教師あり基準線を上回る性能を示す。

## 2. Related Work (関連研究)

画像・動画編集の従来研究は、ペア付きデータの収集困難という課題に直面していた。NP-Edit等の先行研究は外部VLM（Vision Language Model）フィードバックに依存していたが、本研究ではこれを排除。Straight-Through Estimation（STE）はニューラルネットワークの非微分可能操作を通じた勾配流を実現する既知手法であり、本研究はこれを潜在空間編集に応用。サイクル一貫性（cycle consistency）は従来のCycleGAN等で活用されたが、本研究は「勾配ルーティング（gradient routing）」により訓練と推論のギャップを橋渡しする点が新規。

## 3. Method (手法)

提案手法は3つの主要コンポーネントで構成される。

**(1) ノイズ付き疑似ターゲット生成**: EMA（Exponential Moving Average）モデルのコピーを使用し、訓練モデル自身が編集入力をブートストラップ生成。自己の予測から訓練信号を抽出することで「鶏卵問題」を解決。

**(2) 事前学習済みT2Iモデルによる指示追従**: 凍結されたテキスト-画像モデルに対して編集方向のベクトル差分を活用。ソースプロンプトとターゲットプロンプトの予測速度差を利用し、「方向性損失（directional loss）」で編集方向を制約。

**(3) 勾配ルーティングによる構造保存**: 訓練時は1ステップ予測の勾配を流し、推論時は完全に脱ノイズ化された出力を条件付けすることで、訓練推論ギャップを緩和。サイクル損失（cycle loss）で逆編集時のソース画像復元を強制し、不要な情報削除を防止。

## 4. Experiments (実験)

**(1) 動画編集**: Wan2.2テキスト-動画モデルに適用。ユーザー研究で75.3%（±2.2%）の選好率を達成。未見の3D-CGIデータに対しても85%の高い一般化性を示す。

**(2) 長尾スタイル編集**: GTA V、Minecraft、低ポリ3D等6つの非標準スタイルで評価。訓練データに含まれない未見スタイルでも、教師あり基準線（Kontext、Qwen-Image-Edit）を上回る意味的一貫性を達成。

**(3) 一般画像編集**: GEdit-Benchで背景変更・色変更等11カテゴリで評価。KontextとのComparativeで動作・人物・スタイル変更で優位性を示す。物体削除タスクは監督信号が弱い弱点として認識。

## 5. Conclusion (結論)

ペアデータなしでビジュアル編集モデルを訓練するフレームワークを提示。凍結T2Iモデルからの指示追従と勾配ルーティングによるサイクル一貫性の組み合わせで、数百万ペア学習モデルと同等以上の性能を達成しつつ未見領域への一般化を実現。データ収集コストの大幅削減という実用的意義も大きい。
