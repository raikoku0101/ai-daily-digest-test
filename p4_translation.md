## 1. Introduction (はじめに)

LLMエージェントはソフトウェアツール・ビジネスサービス・ローカルワークスペースを横断した実務完結が期待されています。しかし既存のエージェントベンチマークは「リリース時に固定化されたタスク集合と最終応答のみの評価（final-response grading）」という根本的課題を抱えており、進化するワークフロー需要（evolving workflow demand）への適応評価や実際のタスク実行検証が困難です。本論文はこの評価メカニズムを根本的に再設計する必要性を示します。

## 2. Method (手法)

Claw-Eval-Liveは「更新可能なシグナル層（refreshable signal layer）」と「再現可能なリリーススナップショット（reproducible release snapshot）」を分離する設計を採用しています。公開ワークフロー需要信号からClawHub Top-500スキルを抽出し、固定化された環境（fixture・サービス・ワークスペース・採点器）で管理されたタスクを構築。採点は実行トレース（execution trace）・監査ログ（audit log）・サービス状態・ワークスペース成果物を記録し、決定的チェック（deterministic check）と構造化LLM判定を使い分けることで「検証可能なエージェント行動（verifiable agent behavior）」に基づく評価を実現しています。

## 3. Experiments & Results (実験と結果)

105タスク（ビジネスサービス・ローカルワークスペース修復）を対象に13の最先端モデルを評価。最高性能モデルでも「タスク合格率66.7%に留まり、70%に達するモデルが存在しない」という結果が判明しました。失敗パターンはタスクファミリーと実行面で構造化され、HR・管理・マルチシステムワークフローが「持続的ボトルネック（persistent bottleneck）」である一方、ローカルワークスペース修復は相対的に容易という重要な知見が得られています。

## 4. Conclusion (結論)

リーダーボード順位のみでは不十分で、「外部需要信号（external demand signal）と検証可能なエージェント行動の両面でグラウンドされるべき」というワークフローエージェント評価の新原則を提示。ライブベンチマークという概念が AI エージェント評価の新標準となる可能性を示す先駆的研究です。
