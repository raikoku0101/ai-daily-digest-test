## 1. はじめに (Introduction)
大規模推論モデル(LRM)の性能向上にはテスト時計算スケーリングが重要だが、並列サンプリングは極めて非効率。「どれだけ使うか」から「どこに配分するか」へ問題をシフト。固定ハードウェア予算下での部分軌跡(partial trajectories)への制約付き計算配分問題として推論を定式化。

## 2. 関連研究 (Related Work)
自己一貫性(Self-Consistency)が標準的実装だが非効率。Tree-of-Thoughts・MCTSは非同期性がLLM推論と不適合。DeepConf・STEPなど削減型剪定手法は低品質軌跡を終了するが解放コンピュートを再配分しないため出力分布を十分シフトできない。

## 3. 動機付け (Motivation)
高品質な接頭辞(prefix)は正解・失敗経路が早期に分岐する。64並列軌跡を中断し隠れ状態スコアラーでランク付けして最高スコア接頭辞から分岐させると87.5%のpass@1(ベースライン6.2%比14倍)を達成。同時にメモリ消費が半減。大規模バッチでの長文脈推論はKVキャッシュ飽和でレイテンシが約3倍に膨張する問題も解決対象。

## 4. 手法: Gambit (Method)
思考レベルビームサーチで制約付き計算配分を解決。各軌跡を改行区切りのステップシーケンスとしてモデル化し固定容量C(最大同時軌跡数)を保持。Δステップごとに全活動軌跡をスコア関数でランク付けし最低スコアK個を削除して最高スコア接頭辞からK個を分岐(ゼロサムメモリ不変性)。ウォームアップ閾値wで初期ノイズに対抗。論理探索と物理実行を分離する「分離メモリ管理」でメモリ圧力下での崩壊を防止。

## 5. 実験 (Experiments)
Qwen3-4B・DeepSeek-R1-8B・Phi-4-14Bの3モデルで、AIME(2025/2026)・HMMT(2024/2025)・GPQA-Diamondで評価。各問題に256トレースの厳密な予算を配分。自己一貫性・Slim-SC・DeepConf・STEPと比較。

## 6. 結果 (Results)
STEP比+3.3%精度向上(AIME-25)、HMMT-24比+6.7%を達成。HMMT-25で68.5%のトークン削減、標準並列サンプリング比2倍以上の高スループットを実現。システムオーバーヘッド<1%でハードウェア飽和を継続的に維持。精度と効率の両面で従来手法を厳密に支配。

## 7. 結論 (Conclusion)
テスト時推論を部分軌跡への制約付き計算配分問題として再定式化。高品質中間状態への動的計算再配分により複数モデル・ベンチマーク間で一貫した精度向上とトークン効率向上を実証。並列サンプリング・削減型手法の両者を支配する新しいパラダイム。
