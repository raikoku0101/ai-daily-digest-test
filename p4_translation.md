## 1. Introduction (はじめに)
コンピュータ使用エージェント（Computer-Use Agent）は、人間の代わりにPCを操作してタスクを完了する AI システムである。主なアプローチとして、スクリーンショットのみを入力として視覚的に操作する「GUIエージェント」と、コマンドライン・APIなどのスキルを活用する「CLIエージェント（スキルベース）」が存在するが、各アプローチのボトルネックは不明確だった。

## 2. Benchmark Design (ベンチマーク設計)
GUI と CLI エージェントを公平に比較するため「対応ベンチマーク（Matched Benchmark）」を設計。同一タスクをGUI形式とCLI形式の両方で実行可能なタスクセットを構築。タスクは複数のドメイン（ファイル管理、ウェブブラウジング、データ処理）にわたる。

## 3. GUI Agent Analysis (GUIエージェント分析)
スクリーンオンリーのGUIエージェントは、短期タスクでは高い成功率を示すが、長期タスク（Long-Horizon Tasks）で急激に性能が低下。主なボトルネックは「根拠付きインタラクション（Grounded Interaction）」の困難さ—スクリーン上の要素を正確に特定し、意図通りに操作する能力の限界。

## 4. CLI Agent Analysis (CLIエージェント分析)
スキルベースのCLIエージェントは、利用可能なスキル（コマンド）が定義されたタスクでは高性能を発揮。しかし「スキルカバレッジ（Skill Coverage）」の欠如、つまり必要なスキルが事前に定義されていない場合に大きく性能が低下。

## 5. Results (結果)
GUIエージェントの主ボトルネック: 長期タスクでの根拠付きインタラクション精度（特に動的UIへの対応）。CLIエージェントの主ボトルネック: スキル定義外のタスクへの対応不能。両アプローチのハイブリッドが最良の結果を示す可能性を示唆。

## 6. Conclusion (結論)
GUI vs CLI の優劣は一般的に決定できない。タスクの特性（長さ、動的UI依存度、スキル定義可能性）に応じた使い分けが重要。今後のコンピュータ使用エージェント開発において、両アプローチの統合が有望な研究方向として示される。
