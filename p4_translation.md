## 1. Introduction（はじめに）

Transformer言語モデルの残差ストリーム（residual stream）は従来、単一の経路として機能してきた。本論文は、この設計に対して複数の並列ストリームを導入するHyper-Connections（HC）の手法を拡張する。既存のHC関連手法はN=4で頭打ちになるが、著者らはこれを「情報供給ボトルネック」と「計算コストボトルネック」として診断。xHCは時間的特徴拡張と疎な残差ストリーム更新によりN=16まで意味のある拡張を実現し、18B/28B MoEモデルで一貫した改善を示す。

## 2. Related Work（関連研究）

クロスレイヤー情報フロー改善に関する従来研究を整理。残差接続の基本設計から、ゲート機構やDepth-wise重み付けなどの最適化手法、およびMixture-of-Experts（MoE）などの疎計算手法を概観。著者らの貢献は「残差ストリーム」という新しい次元での疎性適用であり、既存のHC/mHC手法との比較において、大規模展開を実現する初めての試みである点を強調している。

## 3. Method（手法）

**基礎概念**: HC手法は複数のN個並列ストリームX_l ∈ ℝ^{N×C}を維持し、事前・事後・残差マッピングで制御。mHCはSinkhorn-Knopp正規化により安定性を確保している。

**ボトルネック分析**: mHCでN>4の場合、各層は単一の書き戻し信号outしか提供せず、複数ストリームが同一情報を競合する「情報ボトルネック」と、ℋ_res生成がO(N³C)の計算量を要する「コストボトルネック」が存在する。

**xHC設計**: (1)時間的特徴拡張—MLP層出力に多スケール因果畳み込み（kernel sizes {4,8,12}）を適用し、Gram-Schmidt直交化で冗長性を除去。(2)疎残差ストリーム更新（sparse residual stream update）—全N個ストリームの読み込みは密（dense）に保ち、更新はk=4個アクティブストリームのみに限定。これによりマッピング生成コストをO(k³C)に削減。

## 4. Experiments（実験）

18B・28B MoEモデルでの事前学習実験を実施。下流タスク評価（MMLU、推論、コード理解など）で、18BでxHCは平均スコア44.8→48.8に改善。スケーリング則実験で、バニラベースラインと比較してxHCは同等損失達成に1.50×計算量を要する。N-sweep実験ではmHCが32%FLOPs増加で損失0.006改善に対し、xHCは4%増加で0.012改善を実現。Muon最適化器との互換性も確認。

## 5. Practical Deployment（実用化）

xHCは28B規模で3.0%のFLOPs追加だが、メモリトラフィックは増加する。xHC-Flashは複数サブレイヤーで全状態操作を共有し、メモリトラフィックをmHC-N=4相当に維持しながら損失面でmHCを上回る。Tritonカーネルを用いたマッピング生成と適用の融合により、中間テンソルの物質化を削減し、実用展開を可能にしている。

## 6. Conclusion（結論）

xHCは時間的特徴拡張と疎更新アーキテクチャにより、N>4での残差ストリーム展開を「有効かつ実現可能」にした初の手法。計算効率改善と一貫した下流性能向上、および複数最適化器への対応により、「展開率」をLLM事前学習の実用的スケーリング軸として確立した。xHC-Flashはメモリ・実装オーバーヘッドをさらに低減し、実用展開を可能にしている。
