# VIA-SD 詳細翻訳

## 1. はじめに (Introduction)
LLM (大規模言語モデル) の推論コスト削減を目的とした Speculative Decoding (投機的デコーディング) は、軽量なドラフター (draft model) が候補トークンを生成し、大規模なベリファイア (verifier) が並列検証する手法。しかし既存手法はバイナリな accept/reject 判定に限られており、中程度の信頼度のトークンに対して大規模モデルを無駄に呼び出していた。VIA-SD はこの非効率を解消するため、多段階検証フレームワークを提案する。

## 2. 問題設定と動機
既存の SD では、ドラフターが拒否されたトークンのうち多くが、フル大規模ベリファイアを使わずとも正しく検証できることが判明した。これにより、「フルモデルより小さいスリムサブモデルで中程度の信頼度トークンを処理できる」という洞察が生まれた。イントラモデルルーティング (intra-model routing) でベリファイアの内部からスリムサブモデルを動的に導出することがキーアイデア。

## 3. VIA-SD フレームワークの手法
VIA-SD は三段階の検証パイプラインを構成する。まず高信頼トークンはドラフターによる直接受理。次に中信頼トークンはイントラモデルルーティングで導出したスリムベリファイアで処理。最後に低信頼トークンのみフル大規模ベリファイアで再計算する。このルーティング戦略により、高コストなフルモデル呼び出し回数を大幅削減する。

## 4. 実験結果 (Experiments)
複数の LLM ベンチマークで評価した結果、VIA-SD は既存の SD ベースラインと比較して拒否率を 0.10〜0.22 削減。既存 SD ベースラインより 10〜20% 高速化し、非ドラフティングデコーディング (autoregressive decoding) と比較して 2.5〜3 倍の加速を達成した。スリムベリファイアによる中間段階の導入が、精度とコストのバランスを改善することを実証。

## 5. 結論 (Conclusion)
VIA-SD は、バイナリ判定に縛られていた Speculative Decoding を多段階検証へと拡張した。イントラモデルルーティングによりフル大規模モデルの呼び出しを削減し、LLM 推論の速度・コストのトレードオフを改善する実用的なフレームワークを提供する。今後はより多様なアーキテクチャへの適用とルーティング戦略の最適化が課題となる。
