## 1. Introduction (はじめに)

大規模言語モデル（LLM）エージェントの手続き的知識は、推論時に読み込まれるスキルパッケージとして保存される。ライブラリ規模の増加に伴い、限定的なコンテキスト予算内で「最小限で十分な実行可能コンテキストをどう提供するか」が中心課題となる。既存システムはスキル全体を検索単位とするため、スキル内の部分的再利用を見落とし、圧縮時に手続き的契約を保存できず、ライブラリ進化にも対応できない。本研究は「セクションレベルの契約保存グラフ圧縮」によりこれら問題を解決する。

## 2. Related Work (関連研究)

スキルベース手続き記憶およびスキル検索・実行システムの既存研究では、パッケージレベルの再利用が標準的。グラフ圧縮・サマリー化技術（頻出部分グラフマイニング、MDL基盤要約、文法ベース置換）は基盤を提供するが、エージェントスキルライブラリには「実行インターフェース、依存関係閉包、検証器到達可能性、ソース原点保持」の保存が必須であり、これらを統合する提案は未だない。

## 3. Method (手法)

SkillZipは4段階の手続き抽象化フレームワークを構成する。

**Sec2Graph**: スキルパッケージを意図（Intent）、入力（Input）、操作（Operation）、検証器（Verifier）、出力（Output）などの型付きセクションノードに分解し、ソースポインタと契約（入出力署名、資源、ガード条件）を抽出する。

**MotifZip**: 「契約保存圧縮」により、反復する型付き部分グラフを可逆マクロに昇格させる。候補は実行インターフェースから生成され、依存関係閉包と検証器到達可能性を保証するもののみ受理される。圧縮利得、再利用スコア、実行リスクに基づきマクロを選定する。

**PathHydrate**: クエリから意図アンカーを抽出し、セクションレベル・スキルレベルの相互参照で種子集合を構築。コンテキスト予算内で依存関係を閉じた手続き部分グラフを検索し、必要な段階でマクロを段階的に展開する。

**ReZip**: 新スキルの到着と実行トレース（展開頻度、検証器失敗、修復コスト）に応じ、既存マクロの再利用、新マクロ昇格、危険マクロの分割・廃止を行う。

## 4. Experiments & Results (実験・結果)

SkillsBench および ALFWorld で評価。MiniMax-M2.7 では SkillZip が報酬 33.3（SkillDAG比+6.0）、gpt-5.2-codex では 43.0（+6.2）を達成。Ret@1 の改善が顕著（MiniMax で +6.9）。圧縮比 3.46× で依存関係保存率（DPR）99.2%、検証器到達可能性（VR）98.7% を保持。セクションレベルノード削除で報酬が 5.4 低下することから、細粒度がコア貢献であることを確認。100K スキルスケールでも Ret@1 65.1 を維持。

## 5. Conclusion (結論)

SkillZip は「セクション依存グラフ → 契約保存圧縮 → 予算対応実行コンテキスト」を統一表現として機能させ、スキルライブラリのスケーラビリティと実行可能性を両立する。複数 LLM バックボーン上で機能し、大規模エージェントシステムの実用化に向けた重要な基盤技術を提供する。
