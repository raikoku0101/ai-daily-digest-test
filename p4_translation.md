## 1. Introduction（はじめに）
現代の LLM ベースエージェントは ChatGPT のような初期の指示チューニング（instruction-tuning）モデルの枠組みから本質的に変わっていない。単一の計算ストリーム内でメッセージを逐次交換する形式に制限されており、「読みながら行動できない」「書きながら新情報に反応できない」「思考しながら行動できない」というボトルネックを抱える。本研究はこれを複数の並列計算ストリームへの指示チューニングで解決する。

## 2. Method（手法）
Multi-Stream LLM は各役割を独立したストリームに分割する新設計。従来の逐次メッセージ交換ではなく、言語モデルの各フォワードパス（forward pass）で複数の入力ストリームから同時に読み取り、複数の出力ストリームに並行してトークンを生成する。すべての出力は因果依存性（causal dependency）を保持し、前のタイムステップに依存する構造。実装は完全にデータドリブンな変更であるため既存アーキテクチャへの統合が容易。

## 3. Benefits & Applications（利点と応用）
並列化による効率改善（efficiency improvement）、関心の分離による強化されたセキュリティ（improved security through separation of concerns）、モニタビリティの向上（improved monitorability）という三つの主要な利点がある。コーディングやコンピュータ操作エージェントのように長時間・複合的な作業を行うシステムでの応答性と安全性の向上が期待される。コードは GitHub で公開済み。
