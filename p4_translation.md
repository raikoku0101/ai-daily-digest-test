# Perceptual Flow Matching for Few-Step Generative Modeling — 詳細解説

## 1. Introduction (はじめに)

Flow Matching（FM）モデルの生成速度を大幅に改善するPerceptual Flow Matching（PFM）を提案する。従来のFlow Matchingは35〜50ステップの多数のサンプリングが必要で計算コストが課題だった。PFMはVAE潜在空間での速度回帰の代わりに、事前訓練された知覚モデルから抽出した知覚特徴空間での監督を行うシンプルな変更により、わずか4〜8ステップで高品質な生成を実現する。教師モデルや補助ネットワークを不要とし、標準的なFlow Matching訓練パイプラインに最小限の修正で統合できる。

## 2. Related Work (関連研究)

Few-Step生成の既存アプローチはProgressive Distillation、Consistency Models、DMD（Distribution Matching Distillation）などの蒸留ベース手法が主流。これらは教師モデルと一貫性制約を必要とし、実装が複雑でモード崩壊（mode collapse）を招く傾向があった。知覚損失（Perceptual Loss）は画像復元で採用されていたが、本研究はこれをFew-Step生成の新機構として活用する革新的アプローチを取る。

## 3. Method (手法)

**基礎: Flow Matchingの原理** — ノイズ分布からデータ分布への連続的な輸送写像を学習。補間サンプルから速度場を予測し、MSE目的関数で最適化される。

**PFMの核心** — 標準FMはx₀の予測を潜在空間で監督するのに対し、PFMは「予測された潜在表現をVAEデコーダで画像空間に復号し、事前学習済み知覚モデルφ(·)の特徴空間で監督」する。ℒ_PFM = E[d(φ(ŷ₀), φ(y₀))] という知覚損失を適用。推論時は一貫性サンプリングを用いステップ数を自由に調整可能。

**なぜ知覚空間が機能するか** — 標準的なEuclidean目的関数では最適予測が事後分布の平均（多峰性分布でぼやけを生む）となる。一方、自然画像で事前学習された知覚モデルはぼやけなどの非多様体的な劣化に大きな距離を割り当てるため、知覚監督はぼやけた平均より有効なサンプルを優先する。

**CFG組み込み** — Classifier-free Guidance（CFG）を訓練時に統合する2戦略を提案: 条件付き/無条件予測を混合してから知覚損失を計算する手法と、CFG強化されたターゲットを目標として使う手法。

## 4. Experiments (実験)

テキスト→画像生成（COCO 2014）、画像編集（MagicBrush）、テキスト→映像生成（VBench）の3タスクで評価。使用モデルはSD3-Medium（画像）、Qwen-Image-Edit（編集）、Wan2.1-1.3B（映像）。

8ステップでFID 33.93、CLIP 31.70、HPSv3 11.42を達成しLCMやDMD2を上回る。画像編集では40ステップベースラインと同等以上の品質を8ステップで実現。VAE潜在空間監督と比較してぼやけが大幅に減少。ランダムViTは失敗し、学習表現が本質的に重要であることを確認。

## 5. Conclusion (結論)

PFMは監督空間のみを変更することでFlow Matchingの数ステップ生成能力を革新的に向上させる。教師モデルやスコアネットワーク不要、スケーラビリティ優秀、蒸留法よりアーティファクトが少ないという複合的な利点がある。1〜2ステップレジームでの性能低下と、最適な知覚空間の理論的特性付けが今後の課題。
