## 1. Abstract（要旨）
トランスフォーマーベース LLM の長文脈処理課題に取り組む。「睡眠（sleep）メカニズム」を提案：モデルが定期的に最近のコンテキストを永続的な高速重み（fast weights）に変換してから KV（Key-Value）キャッシュをクリア。睡眠フェーズでは状態空間モデル（SSM）ブロックが学習したローカルルールで重みを更新。推論時は計算が睡眠フェーズにシフトし、起動時の予測レイテンシーを保持。

## 2. Introduction（はじめに）
LLM の注意（Attention）計算はコンテキスト長に対して二次的に成長し、KV キャッシュメモリは線形に成長するという根本課題がある。ハイブリッドモデル（SSM＋Attention）は固定サイズのメモリを持つが、「削除されたコンテキストに対する深い推論計算」が必要な場面で性能が低下。本研究はオフラインの再帰的処理（offline recurrent processing）でこの問題を解決する。

## 3. Method（手法：LLM Sleep）
提案手法：モデルが定期的に N 回のループを通じて SSM の高速重みを再帰的に更新してから KV キャッシュをクリア。
- 統合フェーズ（consolidation phase）：損失マスクゼロの間、複数の前向きパスでコンテキストを処理
- 予測フェーズ（prediction phase）：単一の前向きパスで回答を予測
訓練時は全体計算グラフを通じてバックプロパゲーションを実行し、削除されたコンテキストから高速重みへの変換を学習。

## 4. Experiments（実験）
- セルオートマトン（Rule 110）タスク：展開ステップ t=32 で「ループなし約 10%」vs「4 ループで 30% 以上」
- Depo タスク（多ホップグラフ検索）：4 ホップ以上の複雑クエリでループ増加により学習速度・最終精度が改善
- GSM-Infinite（合成数学推論）：Ouro 1.4B で 8 操作問題の精度が 31.0%→27.2% に改善

## 5. Conclusion（結論）
コンテキスト削除前に複数の再帰的前向きパスで高速重みを改善する「睡眠」プロセスを提案。制御タスクと現実的な数学推論ベンチマークで睡眠期間増加が深い順序計算の性能を向上させることを実証。生物の睡眠-覚醒サイクルと類似した機構が AI にも有効であることを示した。
