## 1. Introduction (はじめに)
リアルタイム音声アプリケーション（会議システム・補聴器・ゲーム音声等）はそれぞれ異なるレイテンシ（遅延）予算を持ち、従来は各シナリオごとに専用の音声強化モデルを訓練する必要があった。開発・運用コストが高く、単一モデルで複数要件を満たす「ユニバーサル」な音声強化システムの構築が求められていた。

## 2. Method (手法)
「アルゴリズム遅延（Algorithmic Latency）」と「計算遅延（Computational Latency）」の両方を明示的に制御する設計を採用。アルゴリズム遅延は設定可能な先読みフレーム数（look-ahead frames）で調整。計算遅延はEarly-exit機構（早期終了）によりネットワークの異なる深さで推論を完結させることで実現。異なるパディング設定による非効率を避けるため、並列畳み込み層（Parallel Convolution Layers）を導入。

## 3. Training Strategy (訓練戦略)
効率的な訓練のため2段階訓練戦略を採用。共有エンコーダから複数デコーダへの遷移を含む構成で、専門特化型モデルと柔軟なモデル間の性能差を縮小することに成功した。

## 4. Results (結果)
単一モデルで複数レイテンシ要件を満たしつつ、各要件専用モデルと同等以上の音声品質を達成。会議システム・補聴器・ゲーム音声など多様な用途に1モデルを展開可能であることを実証。

## 5. Conclusion (結論)
モデル重みは「huggingface.co/nvidia/Real-time_RE-USE」で公開。開発・運用コストを大幅削減しながら高品質な音声強化を実現する実用的なソリューション。リアルタイム音声処理の今後の標準手法になりうる成果。
