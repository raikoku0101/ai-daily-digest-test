## 1. Introduction（はじめに）
自動回帰生成モデルでのトークン構造がテスト時検索（test-time search）の効果性に与える影響を調査。従来の2次元グリッドトークナイザーと異なり、最近の1D順序付きトークナイザーは粗から細（coarse-to-fine）の階層構造を持ち、初期トークンが全体構造を、後期トークンが詳細を担う。中間状態が意味的に解釈可能なため検証器による効果的な誘導が可能という仮説を検証。

## 2. Background（背景）
自動回帰生成は条件付き確率の積として定式化。テスト時検索は学習済みモデルを事前分布として使用し検証器で候補を評価する推論時スケーリング技術。FlexTokやSemanticicistなどの1D順序付きトークナイザーはネストされたドロップアウト（nested dropout）で訓練され、任意長プレフィックスから有効画像を復元可能。2Dグリッドトークンでは初期トークンが画像の一部領域のみを表現するため全体意味情報が限定的。

## 3. Coarse-to-Fine Structure（粗から細への構造）
FlexTok語彙から異なる最初のトークンを選択・復元すると植物・バッグ・食物・家具などの意味的コヒーレントなクラスターが形成されることを視覚的に確認。各トークンエントリが全体的な意味カテゴリに対応していることを示唆。テキストプロンプトとの整合性を最大化するトークンをビームサーチで直接探索することで学習不要のテキスト-画像生成が可能（2Dグリッドやランダム順序では実現困難）。

## 4. Method: SoTo Framework（手法）
Search-over-Tokens（SoTo）フレームワークは3要素から構成:
検索アルゴリズム: Best-of-NN sampling（並列可能だが非効率）、Beam Search（中間状態の解釈可能性が鍵）、Lookahead Search（より完全な画像で評価・計算コスト増）。
検証器カテゴリ: (1)画像-テキスト整合性（CLIP/ImageReward/PickScore/HPSv2等）、(2)画像-画像整合性（DreamSim）、(3)画像品質（美的スコア）。アンサンブル検証器も探索。
事前分布: テキスト条件付きAR・無条件AR・均一事前分布の段階的誘導レベルを比較。

## 5. Experiments（実験）
制御実験: FlexTok（1D順序付き）vs 2Dグリッドで、ビームサーチが1D順序付きトークンで大幅に大きな改善をもたらすことを確認。
Janusとの比較: 競合2Dグリッドモデルと比較してFlexTokがより強いテスト時スケーリングを示した。
ゼロショット制御: DreamSim検証器によるビームサーチで訓練時未見タスク（DreamBench++）においてDINO-I +18.4の大幅改善。
スケーリング: 530Mモデルに十分なテスト時計算を与えるとリソース制約下の3.4Bモデルを上回る性能を達成。
検証器分析: 8種類すべての検証器で一貫した改善。HPSv2とImageRewardが最高平均ランク。

## 6. Conclusion（結論）
1D順序付きトークナイザーの粗から細への構造がテスト時検索の効果性を大きく向上。限界: 検索アルゴリズムの最適化余地、検証器の信頼性・詳細フィードバック不足、復号化の計算効率、他モダリティへの一般化可能性。
