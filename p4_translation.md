## 1. Introduction (はじめに)
VLM は数百〜数千の視覚トークンを処理するためデコーダ推論コストが高い。既存の「rank-and-remove」手法は低スコアのトークンを永続的に削除するが、本論文はトークンの重要度がデコーダの深さによって変化することを実証。層 3 で低スコアだったトークンが層 25 で高い重要度を示す現象を観察し、これが視覚グラウンディングの崩壊を招くことを指摘する。

## 2. Related Work (関連研究)
デコーダ側の視覚トークン削減手法（FastV、PDrop、Nüwa 等）はすべて不可逆的削除を採用。Mixture-of-Depth（MoD）等の条件付き計算は、トークンが層をバイパスしつつ後続層で再評価される可能性を提供する。本研究はこの概念を VLM 視覚トークンに適用し、訓練不要なルーターとして既存の注意スコアを再利用する点が革新的。

## 3. Method - Reroute (手法詳細)
各ルーティング段階で、テキスト・ビジョン注意スコアを用いて視覚トークンをランク付け。選択されたトークンは Attn+FFN を通過し、遅延トークンは Residual パスでバイパスされ次の段階の候補プールに再参入。従来削減 𝒞ᵢ₊₁ = 𝒱ⁱˢᵉˡ（削除）に対し、Reroute では 𝒞ᵢ₊₁ = 𝒱（全トークン保持）。FLOP と KV キャッシュ予算は同一のまま回復可能性を向上させる。

## 4. Experiments (実験設定)
LLaVA-1.5-7B、Qwen2.5-VL-7B、Qwen3.5-9B-Hybrid の 3 バックボーンで評価。RefCOCO シリーズ（視覚グラウンディング）と複数の VQA ベンチマークを使用。既存削減手法（FastV、PDrop 等）に Reroute を後付けアドオンとして適用して比較。

## 5. Results (実験結果)
88.9% トークン削減下で PDrop+Reroute は視覚グラウンディング IoU を 22.2% から 34.0% に改善し、一般 VQA 性能も維持。Qwen3.5 では 66.7% 削減時に FastV+Reroute が平均比率を 31.1% から 77.5% に改善。ルーティング周波数と層配置の Ablation study は繰り返し再評価の重要性を示す。

## 6. Conclusion (結論)
視覚トークン削減を不可逆的削除でなく「回復可能なルーティング」として再解釈する新パラダイムを提案。訓練不要・プラグイン適用可能で、特に積極的な削減設定下でのグラウンディング精度を劇的に改善。既存の削減手法との組み合わせで効果を発揮し、将来的には補完的圧縮技術との統合が期待される。
