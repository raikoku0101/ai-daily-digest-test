## 1. Introduction (はじめに)

言語モデル(LM)は従来、語彙サイズに比例した埋め込み行列(Embedding Matrix)を使用してきました。パラメータ効率の向上を目指し、先行研究ではエンコーダのみのモデルでハッシング手法を導入しています。しかし、多対一の衝突(Many-to-one Collisions)は因果言語モデルでの応用を困難にしていました。本研究は、ハッシュベースの自己回帰(Autoregression)を実現するMultiHashFormerを提案します。

## 2. Related Work (関連研究)

埋め込み行列の圧縮技術に関する先行研究が存在しており、エンコーダのみのモデルでハッシング手法が提案されてきました。複数トークンを単一ベクトルにマッピングする方式(Hash-based Embedding)が実現されてきましたが、従来の方法は因果言語モデル(Causal LM)への応用に根本的な制限がありました。多言語対応や語彙拡張に関する研究も進展していますが、パラメータフットプリント(Parameter Footprint)の削減と生成性能の両立は未解決でした。

## 3. Method (手法)

MultiHashFormerは、複数の独立したハッシュ関数により各トークンを「ユニークハッシュシグネチャ(Unique Hash Signature)」として表現します。この署名は離散ハッシュID(Discrete Hash IDs)の短い列で構成されています。Hash Encoderがこの署名を単一の潜在ベクトル(Latent Vector)に圧縮し、Transformerデコーダで処理。Hash Decoderが次のトークンのハッシュシグネチャを生成し、テキストに逆変換します。この設計により従来の埋め込み行列の衝突問題を回避しながら自己回帰が可能になります。

## 4. Experiments (実験)

評価は100M、1B、3Bのパラメータスケールで実施されました。複数のベンチマーク(Multiple Benchmarks)を使用して、MultiHashFormerと標準的なTransformer言語モデルの性能を比較しています。また、多言語語彙拡張シナリオでの対応可能性も検証。パラメータ数を変えることなく語彙(Vocabulary)を拡張できるかどうかを確認する実験設計となっています。

## 5. Results (結果)

MultiHashFormerは複数のベンチマークで標準的なTransformer言語モデルを一貫して上回る性能を示しました。異なるパラメータスケール(100M、1B、3B)全てで提案手法の有効性が実証されています。特筆すべき点として、多言語対応時にもパラメータフットプリントを一定に保ちながら語彙拡張(Vocabulary Expansion)に対応できることが示されており、スケール効率の大幅改善が確認されました。

## 6. Conclusion (結論)

MultiHashFormerは、ハッシュベースの表現を活用することで、因果言語モデルにおけるパラメータ効率と性能の向上を同時に実現しました。複数独立ハッシュ関数による「ユニークハッシュシグネチャ」方式は、従来の多対一衝突問題を解決し、実用的なスケーラビリティを提供します。本研究は語彙規模に依存しない LM 設計という新パラダイムを確立し、多言語対応を含む将来の言語モデル効率化研究に重要な貢献をするものです。
