## 1. Introduction (はじめに)
従来の情報検索システムは、事前計算された文書表現に基づくインデックスを利用してきた。本論文は補完的な視点を提示し、検索エージェント（Search Agent）がコーパスそのものを検索環境として扱い、実行可能なシェルコマンド（executable shell commands）を実行して証拠を発見する新しいアプローチを探索している。このDCI（Direct Corpus Interaction）パラダイムにより、検索の粒度を自由に設定でき、より「外科的な」情報検索が可能になる。特に多段階推論（multi-hop reasoning）や複合的な質問応答において有効性が期待される。

## 2. Method (手法)
GrepSeekは二段階訓練パイプラインを採用する。まず、回答を知るチューター（Tutor）と回答を知らないプランナー（Planner）が協調して「検証済みの因果的に正当な検索軌跡（verified, causally grounded search trajectories）」を生成する。チューターが逆方向で支持根拠チェーンを構築し、プランナーが順方向の推論を模擬する手法により、因果的に正当な軌跡を確保する。自動品質フィルタリングにより将来情報の漏洩を防止する。

監督付き微調整（SFT）でコールドスタートデータを学習した後、GRPO（Group Relative Policy Optimization）により強化学習最適化を実施。報酬はトークンレベルF1重複（token-level F1 overlap）と形式的正当性で構成され、構造的に正当な軌跡のみが学習信号を受け取る。

## 3. Experiments (実験)
7つのベンチマーク（NQ、TriviaQA、PopQA、HotpotQA、2WikiMultihopQA、MuSiQue、Bamboogle）で評価。Qwen3.5-9Bをエージェントモデルとし、21M文書のWikipedia（約14GB）をコーパスとして使用。GrepSeekは4つのベンチマーク（NQ、HotpotQA、2Wiki、MuSiQue）で最高F1スコアを達成し、多段階推論で優れた性能を示す。

## 4. Results & Analysis (結果と分析)
終端から端までのレイテンシは平均8.6秒（LLM生成7.86秒、ツール実行0.81秒）で、インデックス構築コストは1分程度。密集検索器（E5: 3.2時間、Qwen3-4B: 62.4時間）を大きく下回る。RL訓練の進行に伴い、エージェントはより少ないコマンドで多くの文脈を抽出する戦略を学習する。

## 5. Conclusion (結論と今後の課題)
GrepSeekはDCIパラダイムを通じて、コンパクトなオープンウェイトモデルが精密で解釈可能な検索プログラムを学習できることを実証した。多段階推論で有効である一方、表面形式の変動（ダイアクリティカルマーク等）への脆弱性が課題。ハイブリッド検索やファジーマッチングの組み込み、推論トレース圧縮によるデコード効率向上が今後の方向性。
