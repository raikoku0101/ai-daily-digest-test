# Code2LoRA — 章別日本語要約

## 1. Introduction (はじめに)

実装されたコードベースには数千のファイルが存在し、言語モデルはインポート、API、プロジェクト規約を理解する必要があります。従来のアプローチではRAG（検索拡張生成）や依存関係分析により長いコンテキストを入力として注入するか、リポジトリごとにLoRAアダプタを微調整してきました。しかし、これらの手法は計算コストが高く、特に「進化するコードベース」では各コミットで適応器が陳腐化する問題がありました。本研究は、ハイパーネットワーク（hypernetwork）を用いてリポジトリ固有のLoRAアダプタを生成し、推論時にトークンオーバーヘッドなしにリポジトリ知識を注入する「Code2LoRA」を提案します。

## 2. Related Work (関連研究)

パラメータ効率的微調整（Parameter-efficient fine-tuning）分野ではLoRA以降、QLoRA、DoRA、多様なLoRA拡張が開発されてきました。ハイパーネットワークによるLoRA生成に関しては、Text2LoRAやDoc2LoRAが短いタスク説明や単一文書からアダプタを生成していますが、Code2LoRAは「完全なコードリポジトリ」を条件入力とし、かつ全7つの注意機構・MLP射影をターゲットにする点で拡張しています。さらに、本研究は「ソフトウェア進化」を組み込んだ初めてのハイパーネットワーク定式化として、GRU（Gate Recurrent Unit）により逐次的コード差分を集約し、リポジトリの履歴を通じて適応器を更新します。

## 3. Method (手法)

**Code2LoRA-Static**: リポジトリ埋め込みを直接LoRA重みに投影する静的ハイパーネットワーク。Qwen3-Embedding-0.6Bで各ファイルを4096トークンチャンクに分割して埋め込み、ファイルレベル埋め込みを重み付き平均とmax-poolで集約します。2層MLPトランク（GELU活性化）で処理後、7つのモジュールタイプ（q, k, v, o, gate, up, down）ごとに専用出力ヘッドでLoRA行列A_m, B_mを生成します。

**Code2LoRA-Evo**: GRUを用いた逐次型ハイパーネットワーク。初期リポジトリ埋め込みで隠れ状態を初期化し、各コミットの差分埋め込みに対してGRUを1ステップ実行。隠れ状態がコード進化を追跡し、16ステップごとに勾配を切離した切断BPTT（Backpropagation Through Time）で訓練。この設計により、静的スナップショットとは異なり、リポジトリの生涯にわたるアダプタ軌跡を生成します。

## 4. Benchmark — RepoPeftBench

MIT/Apache-2.0ライセンスの604個Pythonリポジトリ（512個インドメイン、92個時間的OOD）から構築。タスクはアサーション完成（assertion completion）：テストファイルから構造化プリフィックスを抽出し、期待されるアサーション値を予測。静的トラック（単一スナップショット）と進化トラック（コミット履歴のリプレイ）の2評価軸を設定。

## 5. Experimental Setup (実験設定)

基本LLMはQwen2.5-Coder-1.5B、bf16精度。LoRA階数r=16、α=32、全28トランスフォーマーレイヤーに対して同一アダプタペアを共有。比較対象：RAG（k=3）、依存関係解析コンテキスト、全パラメータ微調整（FFT）、単一LoRA、リポジトリごとLoRA（上界）。評価メトリックはExact Match（EM）、EditSimilarity、CodeBLEU。

## 6. Results (結果)

**静的トラック**: Code2LoRA-Static はクロスリポ（CR）で 63.8% EM（FFT+RAG比+9.9pp）、インリポ（IR）で 66.2% EM（リポジトリごとLoRA上界64.0%に匹敵）を達成。

**進化トラック**: Code2LoRA-Evo は CR で 60.3% EM（単一LoRA比+5.2pp）、IR で 64.5% EM（リポジトリごとLoRA上界64.2%超過）を達成。GRU集約がコード進化追跡に有効であることを実証。

**OOD汎化**: 時間的OOD保留セット（2025-04-01以降）で Code2LoRA-Evo が 74.1% EM で最高性能。

## 7. Conclusion (結論)

リポジトリ知識をパラメータに注入し、ソフトウェア進化を追跡して更新することは、長い入力コンテキストよりも効果的。Code2LoRA-Static は安定したコードベース向け、Code2LoRA-Evo はアクティブ開発環境に適した設計。RepoPeftBenchはリポジトリレベルPEFT手法評価の基盤として機能します。
