## 1. Introduction (はじめに)
長期タスクエージェント（Long-horizon agent）は、構成要素となる各ステップを個別に解決できる場合でも、全体として失敗することが多い。失敗の主因はモデル能力の不足ではなく、実行インフラ（ハーネス）側の問題、特に状態追跡の喪失・コンテキストの断絶・エラーからの回復メカニズムの欠如にある。本論文では、モデルの重みを変更せずに実行システム自体を改善する「ハーネススケーリング（Harness Scaling）」という新しいアプローチを提案する。

## 2. Method (手法)
StateM（ステートマシン型エージェントランタイム）を実装。4つの主要コンポーネントで構成される：
- **耐久的状態（Durable States）**: 実行状態を永続化し、エージェントが現在の進捗を常に把握
- **フェーズローカルコンテキスト（Phase-local Context）**: 各フェーズに必要な情報のみを提供し、コンテキスト汚染を防止
- **チェック済み遷移（Checked Transitions）**: 状態間の遷移に検証ロジックを挿入し、無効な状態遷移を防止
- **回復可能なランブック（Recoverable Runbooks）**: エラー発生時の回復手順を事前定義し、自動リカバリを実現
これらにより、エージェントが長期タスクでも一貫した実行フローを維持できる。

## 3. Experiments & Results (実験・結果)
Terminal-Bench 2.1（長期ターミナル操作ベンチマーク）での評価：
- GPT-5.6 Sol xhigh + StateM: **95.3%の正解精度**（ raw accuracy）
- DeepSeek-V4 Flash + StateM: 82.7% → **88.1%**へ改善（$38の適応コスト）
- APIコスト: GPT参照実装の$574.68に対して**$15**で同等以上の性能
- BusinessBench: マクロ0.55ポイント、マイクロ1.34ポイントの改善

## 4. Conclusion (結論)
ハーネススケーリングはモデルスケーリングと相補的な改善パスであり、既存のモデルをより信頼性高く・経済的に活用できることを示した。StateMのアプローチは任意のLLMバックエンドに適用可能で、エージェントシステムの実用化において重要なインフラ層の改善として位置づけられる。
