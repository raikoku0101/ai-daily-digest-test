## 1. Abstract (要旨)
本論文は、拡散トランスフォーマー(DiTs)において訓練時の解像度を超えた高解像度画像生成を実現するSEGA(Spectral-Energy Guided Attention)を提案します。既存手法はRoPEの外挿と一様な注意スケーリングを組み合わせますが、周波数特性の異なるRoPEコンポーネントに一律スケーリングを適用するため、グローバル構造と詳細情報のトレードオフが生じます。SEGAは各ノイズ除去ステップで潜在表現の空間周波数構造に基づいてRoPEコンポーネント間のスケーリングを動的に適応させ、構造的一貫性と詳細忠実度の両者を改善します。

## 2. Introduction (はじめに)
DiTベースのテキスト→画像生成モデルは高品質を達成していますが、訓練解像度(1024²〜2048²)を超える解像度では品質が著しく低下します。訓練の追加コストや長いトークン系列の計算複雑性を回避するため、訓練不要な高解像度合成が注目されています。既存手法は拡張された位置グリッドで相対位置オフセットが訓練時の分布から乖離し、注意が過度に希薄化される問題に対応していますが、固定的で内容非依存のスケーリングでは完全解決できません。

## 3. Related Work (関連研究)
高解像度画像合成には訓練ベースと訓練不要なアプローチがあります。訓練不要手法はU-Netベースの「DemoFusion」「FreeScale」や、DiT向けの「I-Max」「HiFlow」などに分類されます。RoPE外挿に関しては言語モデルから適応された「Position Interpolation」「NTK」「YaRN」が提案されており、最近では「DyPE」や「UltraImage」がタイムステップ適応や周波数シフトを導入。本研究は直接推論を焦点とし、スペクトル解析による動的RoPEスケーリングを新たに提案します。

## 4. Method (手法)
SEGAは3つの主要コンポーネントで構成されます。①中間潜在表現のスペクトル解析で軸ごとのエネルギープロファイルと回転不変なラディアルプロファイルを抽出。②各RoPEディメンションの波長に基づき対応周波数帯域のエネルギーを取得し、ディメンション特有の補正係数 sd(a) を導出。③スペクトル平坦性(Wiener entropy)から全体的な振幅係数 σ を計算し、md(a)=mref·(1-σ·sd(a)) という次元ごとのスケーリング係数を生成。低エネルギー周波数帯域では位置識別性を強化し、高エネルギー帯域では過度な増幅を抑制します。

## 5. Experiments (実験)
FluxおよびQwenアーキテクチャ上で、2048×4096、4096×2048、3072²、4096²の複数高解像度設定で評価。既存手法(YaRN・DyPE・UltraImage・HiFlow・I-Max等)と比較し、FID・ImageReward・PickScore・CLIP Scoreでいずれも最高性能を達成。4096²解像度では全評価指標でベースラインを上回り、超高解像度生成の安定性を実証。アブレーション研究で軸別スケーリングと全体係数の両成分が必要であることを確認。

## 6. Analysis (分析)
ノイズ除去進行中のスケーリングマップ可視化により、SEGAが異なるプロンプト間で独自のスペクトル指紋(content-aware scaling pattern)を生成していることが示されました。早期ステップではスペクトルが平坦なため基準スケールに近く、後期ステップで構造が明確になるとスケーリングが適応的に再配分されます。YaRNの注意希薄化と比較して、セマンティック局所性とエンティティ一貫性をより良く保持することも確認されました。

## 7. Conclusion (結論)
訓練不要で内容対応的な解像度外挿手法SEGAを提案しました。周波数認識の注意スケーリングにより、従来手法が誘起していたグローバル一貫性と詳細忠実度のトレードオフを解決。複数の解像度とアーキテクチャで構造・意味論・視覚品質を一貫して改善します。アーキテクチャ変更なしに統合可能な最小限かつ効果的なソリューションは、今後の周波数認識注意修正研究への動機付けとなります。
