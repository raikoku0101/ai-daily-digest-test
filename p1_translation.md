## 1. Introduction（はじめに）
拡散確率モデル（DPM）はSNR-タイムステップバイアスという根本的問題に直面している。訓練時はサンプルのSNRとタイムステップが厳密対応するが、推論時の予測誤差・数値誤差の累積でその対応が崩れる。ノイズ予測ネットワークが不適切なSNRのサンプルに対して不正確な予測を生成し、生成品質が大幅に低下する。

## 2. Related Work（関連研究）
既存研究はExposure Bias（サンプル間の不一致）に焦点を当ててきたが、本論文のSNR-tバイアスはより根本的な「サンプルとタイムステップ間の不一致」を扱う。ADM-IP・ADM-ES・DPM-FRなど既存補正法は訓練が必要または改善が限定的だった。

## 3. Background（背景）
拡散モデルは前向きプロセス（データのノイズ化）と逆向きプロセス（ノイズからデータ復元）で構成される。タイムステップtに対するSNR（Signal-to-Noise Ratio）は数学的に定義され、訓練時にはこの対応が厳密に保持される。

## 4. SNR-t Bias（バイアスの実証的分析）
Key Finding 1: 固定タイムステップsに対してSNR不一致のサンプルを入力すると予測誤差が顕著に増加。低SNRサンプルは過度なノイズ予測を、高SNRサンプルは過小な予測をもたらす。
Key Finding 2: 逆向きプロセスの予測サンプルは同一タイムステップの前向きサンプルより常に低いSNRを示す。

## 5. Method（手法）
Assumption 5.1を導入し再構成サンプルの数学的形式を定式化。Theorem 5.1で逆向きプロセスのSNRが常に前向きより低いことを証明。ピクセル空間での微分補正：各ステップで補正式 x̂ₜ₋₁ = x̂ₜ₋₁ + λₜ(x̂ₜ₋₁ - xθ⁰) を適用。DCW（ウェーブレット補正）ではDWTで周波数成分（ll/lh/hl/hh）を分離し各周波数帯に独立した補正係数λᵗᶠを適用。低周波は減衰・高周波は増加という動的スケジューリングを導入。

## 6. Experiments（実験）
IDDPM・ADM・ADM-IPなどCIFAR-10からLSUN Bedroom（256×256）まで評価。IDDPM on CIFAR-10で20ステップ時FID 42.6%削減、50ステップで25%削減。ADM-ES・DPM-FRへの統合でさらに3〜7%追加改善。FLUX（10ステップ）では過度な平滑化と露出過剰が顕著に軽減。計算オーバーヘッドはわずか0.08〜0.47%。

## 7. Conclusion（結論）
SNR-tバイアスは拡散モデルの根本的問題。提案DCWは訓練不要・プラグイン可能な周波数別補正で様々なDPM・データセット解像度で一貫した品質改善を達成。
