## 1. はじめに (Introduction)
本論文は、ビデオ拡散モデル(VDM)の事前学習済み知識を活用して、複数の視覚モダリティ間での柔軟な動画生成を実現するUniVidXを提案。従来手法は「RGB→アルベド」など固定的な入出力マッピングに制限されていたが、UniVidXは「テキスト→X」「X→X」「テキスト&X→X」の3パラダイムに対応し、単一モデルで15個以上のタスクを処理できる。

## 2. 関連研究 (Related Work)
ビデオ拡散モデルを基礎とした多模態生成が急速発展。固有画像分解やアルファチャネル処理など、グラフィックス基本問題に拡散モデルを適用する研究が増加。しかしタスクごとに別個モデルを訓練する問題があり、モダリティ間相関が無視されていた。

## 3. 手法 (Method)

### 3.1 確率的条件マスキング (SCM: Stochastic Condition Masking)
訓練時に「生成対象となるノイジーなモダリティ」と「条件として機能するクリーンなモダリティ」をランダム分割。固定マッピングから脱却し、様々な方向の条件付き生成を学習させる。目的関数: ℒuni = 𝔼[||vθ(...) - v||²]

### 3.2 分離型ゲート付きLoRA (DGL: Decoupled Gated LoRA)
各モダリティに独立したLoRAモジュール (ΔWk = Bk × Ak) を割り当て。モダリティが生成対象の場合のみ活性化 (mk=1)、条件として機能する場合は非活性化 (mk=0)。パラメータ干渉を防ぎながらVDMの事前知識を保持。

### 3.3 クロスモーダル自己注意 (CMSA: Cross-Modal Self-Attention)
クエリはモダリティ固有のままだが、キー・値は全モダリティから集約。Attention(qi, kshared, vshared) = Softmax(qi × ksharedᵀ/√dk) × vshared によりモダリティ間の密な相互作用を促進。

## 4. 実験結果 (Experiments)
- テキスト→固有画像生成でユーザー評価向上 (RGB: 9.34 vs 比較手法 7.82)
- InteriorVid-Testでアルベド推定PSNR 16.89dB、法線推定平均角誤差 11.09°、既存手法超え
- VideoMatteベンチマークで補助情報なし手法として初めてマスク有導出法 (MAD: 4.24) を上回る

## 5. 限界 (Limitations)
- 固有画像とアルファラベルの同時注釈データが不足し2モデルに分離実装
- メモリ制約により最大4モダリティ・21フレーム・480p解像度に限定
- 透明ガラスなど物理コーナーケースでの不安定性

## 6. 結論 (Conclusion)
事前学習VDMの堅牢な知識を活用しつつモダリティ間一貫性を保証する統一フレームワーク。1000本未満の限定訓練データで複数ドメインにて最先端性能を達成。タスク固有孤立パラダイムからの原則的な転換をもたらす研究。
