## 1. Introduction (はじめに)
デザイナーが日常的に直面する課題は、ラスタ画像から編集可能なデザインファイルを復元すること。現在このプロセスは手作業で時間がかかる。本論文では複数のモーダル属性（テキスト、ベクトル図形、色、グループ化、レイヤー順序）を復元する必要があることを指摘し、レイヤー階層の構造的復元として定式化。

## 2. Related Work (関連研究)
先行研究は3領域に分かれる。①レイヤー分解モデル（LayerD、Qwen-Image-Layered）はRGBAレイヤーへの分解を実現するが編集可能性に不十分。②画像ベクトル化手法（DeepSVG、OmniSVG）はベクトルパスを生成するがテクスチャに不向き。③ツール利用エージェント（ReAct）は長期エラー累積に脆弱。

## 3. Method (手法)
ReDesignはVLMエージェントが根ノードから階層を段階的に拡張するアプローチを採用。
**アクション空間**: テキスト抽出・多層分解・連結成分ラベリング・検出分割・ベクトル化の5ツール鎖。
**制御ポリシー**: VLMが各ステップで拡張ノードとツール選択を決定、並列実行が可能な階層構造を維持。
**段階的検証 (Graceful Verification)**: 各ノード拡張で検証器が提案子ノードを受理・枝刈り・再試行の3択で評価し、エラーを局所化。
**メモリ管理**: 各ノードの直系履歴のみ記録し指数関数的な状態爆発を防止。

## 4. Experiments (実験)
**Figma Edit Replay Benchmark**: 909個の生Figmaファイルと14,796個の制御された編集指示（レイアウト・色・テキスト）から構成。
**ベースライン**: Qwen-Image-Layered、LayerD、VTracer、ReAct型エージェントと比較。
**評価指標**: 編集前後でSSIM（構造類似度）とテキスト認識率を測定。

## 5. Results (結果)
ReDesignは全編集タイプで優れた性能を示した。
- オブジェクトレベル: L1誤差0.0431（最良）、LPIPS 0.0883（最良）
- レイアウト忠実性: PQ値45.37、F1スコア0.535（いずれも最良）
- テキスト復元: 編集後のテキスト認識率が最高
- 並列実行により直列処理比7.1倍の高速化を達成

## 6. Conclusion (結論)
ラスタ画像から編集可能なデザイン構造を復元する実用的エージェント型システムを提案。階層的構造と局所的検証によりエラー累積を防ぎ、信頼性と効率を同時向上。生成型画像編集モデルが進化しても、明示的なオブジェクト・パラメータ・階層を持つ編集可能フォーマットが精密なデザインワークフローに重要であることを示す。
