## 1. Introduction (はじめに)
Transformer の二次計算量は長いシーケンス処理での課題となっており、xLSTM、Mamba-2、Gated DeltaNet といったサブ二乗アーキテクチャが代替案として注目されている。本論文は、これら三つの主要設計を複雑な依存関係を持つデータ領域（コードおよび時系列）で初めて直接比較する。xLSTM 系バックボーンがほぼすべての設定で優れた性能を示す理由を、統一フレームワークと合成タスクを通じて説明することを目的としている。

## 2. Experiments with Complex Dependencies (複雑な依存関係の実験)
三つの実験設定を実施。コード生成言語モデルの事前学習では、xLSTM [7:1] が全学習構成で HumanEval pass@k で一貫して首位。Transformer からの蒸留では、xLSTM [1:0] が平均的により強力なコード学習者を提供。時系列財団モデルの事前学習では、1M〜40M パラメータスケールで xLSTM [3:1] が最高スコアを達成している。

## 3. Analysis of Leading Subquadratic Architectures (主要アーキテクチャの分析)
三つのモデルを共通の線形注意フレームワークで表現。xLSTM は入力・忘却ゲートが独立しており、柔軟な重み補正を可能にする。Mamba-2 はゲートが結合されており、表現力が限定的。Gated DeltaNet は明示的な状態上書き機構を持ち、検索タスクに適しているが累積に課題がある。xLSTM の利点は累積と有限状態追跡の両方の能力を組み合わせることにあると仮説立てる。

## 4. Experiments on Accumulation and State Tracking (累積と状態追跡の実験)
長さ 128 で訓練し 512 と 2048 で評価する合成タスク（カウント問題とパリティ問題）を実施。xLSTM [1:0] は全カウントタスクで長さ汎化が可能だが、状態追跡には失敗。xLSTM [1:1] だけが両方の能力を統合し、両タスク家族で長さを超えて一般化する唯一の構成であることを実証。

## 5. Conclusion (結論)
複雑なデータ領域における xLSTM の経験的優位性は、累積メカニズムと状態追跡機能の組み合わせに由来する。xLSTM は行列状態線形注意と再帰的状態更新を分離することで、他のサブ二乗演算子が達成困難な柔軟性を実現している。今後の研究では、この原則を他のアーキテクチャ設計にも拡張することが期待される。
