## 1. Introduction (はじめに)

従来の検索エージェントは成長する会話記録（トランスクリプト）に対してポリシー（policy）として訓練されており、モデルは検索方法の決定と同時に、観察内容の記憶・有用な証拠の管理・未解決の制約の追跡・検証済みクレームの管理を行う必要がありました。本研究はこの設計が「定型的な状態管理をポリシー内部に過剰に押し込んでいる」と指摘し、RL（強化学習）から切り離せる状態管理を外部化することを提案します。

## 2. Method (手法)

Harness-1は20Bパラメータの検索エージェントで、ステートフルな「ハーネス（harness）」内でRLにより訓練されます。ハーネスは環境側で複数の作業メモリを管理します：
- **候補プール（candidate pool）**: 検索候補を一時保管
- **重要度タグ付き厳選セット（curated set）**: 重要と判断されたドキュメントを保持
- **コンパクト証拠リンク（evidence links）**: 圧縮された根拠情報
- **検証記録（verification records）**: チェック済みクレームのログ
- **予算認識コンテキストレンダリング（budget-aware context rendering）**: トークン予算を意識した文脈構築

ポリシーは「何を検索するか」「何を保持・廃棄するか」「何を検証するか」「いつ停止するか」という意味的判断のみに集中します。

## 3. Results (結果)

8つの検索ベンチマーク（Web検索・金融・特許・マルチホップQA）で評価し、平均キュレーション再現率（curation recall）0.730を達成。次点のオープンソース検索サブエージェントを+11.4ポイント上回り、大規模フロンティアモデルとも競争力を維持。特に保持外転移（held-out transfer）ベンチマークでの高い一般化能力は、外部状態に対するRLが訓練ドメイン外にも転移できることを示唆しています。
