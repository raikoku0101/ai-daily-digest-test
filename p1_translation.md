## 1. Introduction (はじめに)
実際のコードベースは数千のファイルで構成され、モデルはインポート・API・プロジェクト規約を理解する必要があります。従来の方法は「RAGによる長い入力注入」または「リポジトリごとのLoRA微調整」を採用してきましたが、いずれも計算コストが高く、進化するコードベースに脆弱です。本論文は「ハイパーネットワーク」を用いて、リポジトリ固有のLoRAアダプターを生成し、推論時のトークンオーバーヘッドをゼロにする Code2LoRA フレームワークを提案しています。

## 2. Related Work (関連研究)
LoRA（低ランク適応）は効率的なパラメータ調整を実現し、多くの拡張手法が存在します。ハイパーネットワークによるLoRA生成の最新研究として Text2LoRA や Doc2LoRA があり、短いテキスト説明や単一ドキュメントから適応ウェイトを生成します。Code2LoRA は「リポジトリ全体」という新しい入力モダリティを扱い、「ソフトウェア進化への対応」という軸を加え、従来手法を拡張しています。

## 3. Method (手法)
Code2LoRA は三つの構成要素を持ちます：(1) リポジトリコンテキストを密度ベクトルに変換する **リポジトリエンコーダ**（Qwen3-Embedding-0.6Bを使用、ファイル埋め込み + リポジトリレベル集約）、(2) ハイパーネットワーク、(3) 凍結したLLM。Code2LoRA-Static は単一スナップショットを適応ウェイトに直接変換し、Code2LoRA-Evo は GRU 隠れ状態で「コミット差分列」を集約し、時間的に変化するリポジトリに対応します。両シナリオとも推論時オーバーヘッドがゼロです。

## 4. RepoPeftBench (ベンチマーク構築)
604個のPythonリポジトリ（512個は学習用、92個は時間的OOD保留）から「アサーション完了タスク」を抽出した評価基盤を新規構築しました。静的トラック（単一スナップショット・40K訓練タスク）と進化トラック（215Kのコミット派生タスク）を設計し、Cross-Repo と In-Repo の分割を用意しています。

## 5. Experiments (実験設定)
ベースLLMは Qwen2.5-Coder-1.5B、エンコーダは Qwen3-Embedding-0.6B。Code2LoRA は全7つの注意・MLP射影に対してランク16のLoRA を生成（~720M訓練パラメータ）。比較手法は RAG・FFT・Single LoRA・Per-repo LoRA 等。

## 6. Results (結果)
静的トラックで Cross-Repo 63.8% EM を達成（最強ベースライン比 +9.9pp）、Per-repo LoRA 上限（64.0%）にも匹敵。進化トラックで Code2LoRA-Evo は 60.3% CR EM・64.5% IR EM。OOD汎化でも 74.1% EM で最高性能。

## 7. Conclusion (結論)
ハイパーネットワークによるリポジトリ固有LoRA生成により、推論時トークンコスト無しでリポジトリ知識を注入でき、静的・動的両シナリオで最強ベースラインを凌駕。RepoPeftBench は今後の研究基盤となります。
