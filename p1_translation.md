## 1. Introduction (はじめに)
従来のRLHFでは応答全体に単一スカラー報酬を割り当て、各トークンへ均一に分配していた。本論文はこの「均一分配」が問題であると指摘。異なるトークンが異なるルーブリック基準に貢献しているにもかかわらず、情報が活用されていない。提案手法の反事実的再評価（counterfactual replay）により、ルーブリック基準の有無での応答再スコアリングを行い、トークンレベルの信用割り当てを実現する。

## 2. Related Work (関連研究)
ルーブリックベースRL・指示追従の先行研究を広範にレビュー。直接の先行研究RTT（Rubrics-to-Tokens）は別個のトークン関連性判別器学習が必要である点が対比される。PPO・GRPOなどのポリシー最適化手法、過程報酬・ステップレベルクレジット割り当て研究も整理。提案手法は「政策内部の反事実的尤度対比」を使い、別モデル学習を回避する点が差別化ポイント。

## 3. Method: ルーブリック信号とGRPOの統合
標準GRPOは全トークンに一様なグループ相対優位性A_iを適用する。本手法はトークンウェイトw_{i,t}を導入して応答内クレジット再分配を実現。制約として平均ウェイトは1に正規化し、応答レベルの報酬方向は維持。ルーブリック依存的なトークンへの更新シグナルを強化できる。

## 4. CoRT Algorithm (アルゴリズム詳細)
**反事実的スコアリング**: 同一応答を「ルーブリック有り」「ルーブリック無し」両プロンプトで評価し、対比Δ_{i,t} = ℓ^+_{i,t} - ℓ^-_{i,t}でルーブリック依存度を測定。
**リプレイマージンスコアリング**: 対比をシグモイド変換で有界化し、η∈(0.75, 1.25)の重み乗数を生成。
**段階的応答正規化**: SmoothStep関数で訓練初期は標準GRPOに近く、徐々にトークンレベル重み付けを強化。
**ポリシー目的**: 停止勾配（sg）を通じた重みで元の符号付き利点を保持しつつトークン間で再分配。

## 5. Experimental Setup (実験設定)
HIR-16kデータセットで実験。Qwen3-4B-Instruct・Qwen2.5-7B-Instructを使用。報酬粒度はCSR（制約充足率）とAON（完全充足）の2種。評価ベンチマーク：IFBench、IFEval、MultiDimIF、AdvancedIF。設定：プロンプトバッチサイズ64、応答サンプル数8、温度1.0。

## 6. Results (実験結果)
ほとんどの比較でGRPOを上回り、平均4.4ポイント改善。MultiDimIFでは特に顕著で、Qwen3-4B CSRで+6.10pt、Qwen2.5-7B CSRで+11.85ptの大幅改善。RTTと競争力を保ちながら別個の関連性学習段階を不要にした。DAPO・GSPOとの組み合わせでも有効性を確認。

## 7. Training Diagnostics (訓練診断)
アブレーション実験により、応答正規化とSmoothStepランプが訓練安定性に不可欠と確認。正規化なしでは平均トークンウェイトが1.04に上昇、後期訓練で勾配ノルムが急増しクリッピングも増加した。

## 8. Conclusion (結論)
CoRTは軽量なクレジット割り当て手法。別モデル学習不要で、元のロールアウト分布・報酬構造を保ちながらルーブリック依存トークンへのアドバンテージ再分配を実現。構造化基準がスコアリングだけでなく応答内クレジット割り当ての指導にも有用であることを示した。
