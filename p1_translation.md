## 1. Introduction (はじめに)

音声エージェントは、自動音声応答システムとして企業環境で急速に展開されています。しかし既存のベンチマークは、「現実的なシミュレーション対話の生成」と「音声固有の障害モードの包括的な測定」という2つの根本的な課題に同時に対応していません。EVA-Benchは、検証ゲート付きボット間オーディオ対話と、架構に依存しない複合メトリクス（EVA-AとEVA-X）を組み合わせることで、これらの課題に取り組みます。

## 2. Related Work (関連研究)

既存の音声ベンチマークは、STT堅牢性やTTS品質、会話ダイナミクスなど個別コンポーネントに焦点を当てる傾向があります。τ-VoiceやFDB-v2は「ライブボット間マルチターン対話」に最も近いアプローチを提供しますが、「シミュレーター動作の自動検証」および「複数試行にわたる信頼性測定」の機能を欠いています。音声エージェント品質測定では、既存フレームワークはツール選択精度や返答潜時に限定され、「方針忠実性」「音声レベルの実体精度」といった重要な側面を見落としています。

## 3. Methodology (手法)

EVA-Benchは3つの側面から構成されます。**データ設計**では、航空業顧客サービス（CSM）、医療HR（HRSD）、企業IT（ITSM）の3ドメインで213シナリオを設計し、ユーザー目標・ペルソナ・決定木を含めます。**マルチターン対話**では、WebSocketを介したボット間オーディオ通信を使用し、カスケードおよび音声ネイティブ架構を等しく評価します。**シミュレーション検証**は「ユーザー行動忠実性」および「音声忠実性」をLLM/LALM判定で検証し、失敗時は自動再生成します。

メトリクスは3層構造です。EVA-A（精度）は「タスク完了」「忠実性」「音声精度」を統合。EVA-X（体験）は「会話進捗」「簡潔性」「ターンテイキング」を測定。pass@1（平均性能）、pass@k（ピーク性能）、pass^k（信頼性）の3つの集約統計で信頼性過大評価を防ぎます。

## 4. Experiments (実験と分析)

12システム（カスケード7、ハイブリッド2、S2S3）を評価。判定モデルと人間アノテーター間の一致度（κ=0.777-0.845）を確認。主な知見：精度と体験の同時達成が困難（両者で0.5超のシステムなし）、ピーク性能と信頼性の乖離（pass@k-pass^kの中央値0.44）、架構別の非対称な脆弱性（カスケード系はアクセント変化で精度10ポイント低下、S2S系は背景雑音で体験劣化）。

## 5. Conclusion (結論)

EVA-Benchは検証ゲート付きシミュレーション、架構非依存メトリクス、マルチ試行信頼性フレームワークを統合。カスケードとS2S系の「精度では比肩可能だが体験で乖離」という本質的トレードオフを明らかにし、フレームワークを完全オープンソース化。
