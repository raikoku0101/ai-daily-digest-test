## 1. Introduction (はじめに)
本論文は、LLMエージェントが「いつ行動を停止すべきか」という問題を扱う。従来のLLM abstentionは単一ターンの回答/棄却決定だが、本研究では「エージェント的抽象化(Agentic Abstention)」として多段階の対話環境での抽象化を定義する。ウェブショッピング、ターミナル、質問応答の3つの場面で、28,000以上のタスクを用いて13種類のLLMモデルを評価した。結果として、エージェントは指示が実行不可能な場合でも多くの不要な行動を続けることが判明した。

## 2. Method (手法)
本研究ではAgentic AbstentionをPOMDP（部分観測マルコフ決定過程）として定式化し、「回答」「棄却」「行動」という3つの選択肢を考える。エージェントは相互作用履歴に基づいて決定を行う。提案手法「convolve」は、完全な相互作用軌跡から学習し、再利用可能な停止ルールをコンテキストに追加する形でエージェント性能を向上させる。

## 3. Datasets (データセット構築)
WebShop、Terminal-Bench 2.0、AbstentionBenchから計27,000以上のインスタンスを適応。「要求ベース抽象化」（曖昧な指示）と「環境ベース抽象化」（相互作用後に実行不可が判明）の2種類を構成。WebShopでは主観的選好・不十分な意図・矛盾などで249件、環境修正で251件のタスクを作成。

## 4. Results (結果)
多くのエージェントは「結果的には」抽象化するが、必要な時点では抽象化しない。Llama-3.3-70BはWebShopでAbsRec@1が26.7%にとどまり、不要な相互作用が頻発。推論能力の向上が適切な抽象化を必ずしも改善しない。convolve適用後、AbsRec@1は57.4%、AbsRec@10は100%に改善。

## 5. Conclusion (結論)
エージェント的抽象化は、スケーリングや推論強化だけでは解決されない独立した課題。Context engineeringベースのアプローチにより、パラメータ更新なしで重大な改善が可能であることを示した。信頼性の高いエージェントには「いつ行動を続けるべきでないか」の判断が不可欠。
