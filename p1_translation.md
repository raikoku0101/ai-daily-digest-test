## 1. Introduction (はじめに)
拡散モデルを用いた画像生成において、「3D制御の忠実性と光写実性の両立」という課題に取り組む。通常、3D制御（幾何情報やカメラ視点など）の学習には合成データが必要だが、合成データでの微調整は生成画像を不自然に見せてしまう。著者らは、モデルが「制御信号の存在と合成的な外観を結びつける」ことが問題の根本原因と指摘し、これを解決する軽量フレームワーク Realiz3D を提案。

## 2. Related Work (関連研究)
既存研究では、テキスト画像生成モデルに制御能力を付与するため、LoRA層・アダプタ・ControlNet などの手法が提案されてきた。ドメイン適応の観点から Wonder3D・AnimateDiff などのアダプタベース手法も存在する。本研究は「ドメイン識別と制御信号を明示的に分離」する点でこれらと差別化される。

## 3. Method (手法)
二段階の訓練戦略を採用。第1段階：「ドメインシフタ（Domain Shifter）」と呼ぶ軽量残差アダプタを訓練し、実写（real）と合成（synthetic）ドメインを独立に学習。第2段階：「表現バインディング（Representation Binding）」戦略で、早期層は合成データでの更新を制限し後期層は実写データで更新する層認識訓練（Layer-Aware Training）とドメイン再割り当て（Domain Reassignment）を組み合わせ、制御信号の転移性を向上。推論時には実ドメインに設定しながら3D制御を適用可能。

## 4. Experiments (実験)
マルチビュー画像生成（テキスト→マルチビュー）と3D入力→テクスチャリングの2タスクで評価。比較手法：完全微調整・LoRA・既存ドメイン適応手法など複数。提案手法は光写実性において有意な改善を達成しつつ、3D一貫性を維持。

## 5. Results (結果)
定量評価では FID_I（Fréchet Inception Distance）・KID_I（Kernel Inception Distance）など実写類似度メトリクスで他手法を大きく上回った。3D一貫性メトリクス（PSNR・SSIM・LPIPS）は合成専用ベースラインより若干低いものの十分な精度を保持。定性評価でも幾何的忠実性とフォトリアリズムの両立が視覚的に確認された。

## 6. Conclusion (結論)
ドメインシフタ・層認識訓練・ドメイン再割り当ての3手法を組み合わせ、合成データから制御性を学習しながら基盤モデルの写実的特性を保全することに成功。制御信号の若干の精度低下は存在するが、ライティング補正などの将来研究で改善可能。3D生成の実用化に向けた重要な進展を示した。
