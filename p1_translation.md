## 1. Introduction (はじめに)

大規模言語モデル(LLM)の強化学習において、Group Relative Policy Optimisation(GRPO)は効率的なアルゴリズムとして注目されています。しかし、系列レベルの利点をトークンレベルの確率に変換する際に、固定的な集約メカニズムが適応性を制限しています。密集したシグナル(MATH500など)では固定集約が高分散を引き起こし、希疎なシグナル(AIME24など)では信号を過度に平滑化する問題があります。これらの課題に対応するため、動的にパラメータを調整可能な一般化フレームワークが提案されています。

## 2. Related Work (関連研究)

既存手法ではGRPO(算術平均、p=1)やGMPO/GSPO(幾何平均、p→0)など、静的な集約関数のみが検討されてきました。並行研究のPMPOはp∈[0,1]で適応化していますが、本研究はpを全実数範囲に拡張し、時間軸に沿った動的スケジューリング戦略を採用する点で異なります。

## 3. HölderPO: 一般化集約フレームワーク (Generalised Aggregation Framework)

トークンレベルの重要度比 r_{i,t}(θ) を Hölder 平均で集約します。パラメータ p により勾配重み分布を制御し、p=1 でGRPOを、p→0 でGSPOを復元します。Shannon エントロピーが p=0 で最大化され、|p|増加に伴い単調減少することを定理で示します。正の p は高い重要度比を持つトークンに勾配を集中させ、負の p は低い比のトークンを強調します。

動的pスケジューリング戦略として、固定パラメータでは早期信号増幅と後期分散制御を同時達成が不可能であることを証明。p を p_high=2 から p_low=-2 へ線形減衰させることで、早期段階の稀な有用信号増幅と後期段階の安定した収束を実現します。

## 4. Experiments (実験)

Qwen2.5-Math-7B上での評価で、HölderPO（動的スケジュール）が5つの数学ベンチマーク（AIME, AMC, MATH, Minerva, OlympiadBench）で平均54.9%の精度を達成し、標準GRPO（51.2%）を上回ります。p=3固定値でAIME24ベンチマーク46.7%（既存記録43.3%を更新）を達成。

ALFWorldエージェント推論タスクでは93.8%の成功率（GRPOの72.8%から大幅改善）を達成し、より保守的なスケジュール(p:1→-1)がドメイン外タスクに適切であることも確認されました。

## 5. Conclusion (結論)

HölderPOは固定的な集約メカニズムの制限を克服し、Hölder平均を通じた連続的な制御を実現します。動的スケジューリングにより、追加計算コストなしで複雑な推論タスクとエージェント推論で最先端性能を達成。残課題として、スケジュールハイパーパラメータの経験的チューニングと報酬ハッキングへの感度が挙げられています。
