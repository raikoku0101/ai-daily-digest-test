## 1. Abstract（概要）
RLHF（Reinforcement Learning from Human Feedback）はテキスト→画像生成で主流パラダイムとなっているが、画像編集への適用はほぼ未探索。最大のボトルネックは「既存の報酬モデルが全体的スコアのみを出力し、個別の編集指示要件を無視してバイアスある報酬を生じさせる」点。本研究は推論型検証器（Reasoning Verifier）ベースの RL フレームワーク「Edit-R1」でこの課題を解決する。

## 2. Introduction（はじめに）
画像編集タスクでは「オブジェクトの色を変える」「背景を差し替える」「スタイルを転換する」など多様な指示が存在し、各指示は異なる評価基準を必要とする。従来の CLIP スコアや人間評価ベースの報酬は粗粒度で、微妙な編集品質の差を捉えられない。また人手アノテーションはコストが高く、スケールしない。本研究はこれらを自動化された細粒度検証器で解決することを目指す。

## 3. Method（手法）
Edit-R1 は2段階で構成される。①**SFT 初期化（Supervised Fine-Tuning Initialization）**: 高品質な編集例で報酬モデルを教師あり学習し、基本的な編集品質判断能力を習得させる。②**グループ対照的選好最適化（Group Contrastive Preference Optimization, GCPO）**: 同一指示に対する複数の編集出力をグループとして比較し、Chain-of-Thought（思考の連鎖）で各評価原則（忠実度・指示遵守・自然さ等）を個別評価して報酬を計算。RL ループで編集モデルを継続的に改善する。

## 4. Experiments（実験）
FLUX.1 を含む複数の最先端画像編集モデルに Edit-R1 を適用して評価。ベンチマークは Emu-Edit、EditBench、TEDI など標準的な画像編集評価データセット。定量指標（CLIP-I Score, DINO Score, L1 distance）と人間評価の両方で検証した。

## 5. Conclusion（結論）
Edit-R1 フレームワークは推論型検証器による細粒度な報酬計算により、従来のスコアラーベース手法の課題を克服し、FLUX.1 等の最先端モデルの画像編集性能を定量・定性的に改善した。スケーラブルな自動検証器で画像編集モデルを継続改善できる枠組みとして、テキスト→画像編集の RLHF 研究に新たな方向性を示す。今後の課題は検証器のさらなる汎化性向上と動画編集への拡張。
