## 1. Introduction (はじめに)
自己教師あり新規視点合成（self-supervised Novel View Synthesis: NVS）は「ポーズ付き多視点データの極端な希少性」という課題に直面している。既存手法は静的シーン動画の小規模データセットに限定されており、マルチネットワークパイプラインの複雑性により拡張が困難。本論文は単一の統合transformerバックボーンを用いて、動的コンテンツを含む一般的な動画から学習可能なRayDerを提案。動的コンテンツはnuisance factor（迷惑要素）として扱われ、カメラ推定を安定化させながら静的シーンNVSを目標タスクとして維持する。

## 2. Method (手法)
RayDerはRayZerの3つの分離ネットワーク（カメラ推定器・シーン再構成器・レンダラー）を単一バックボーンに統合する。主要改善点として、動的状態埋め込み（dynamic state embedding）をカメラポーズと並行して予測し時間変動コンテンツを分離。並列ターゲットアテンション（parallel target attention）により計算効率を約7倍改善。自己回帰ポーズ学習（autoregressive pose learning）でランダム順序の複数入力ビューから逐次予測し、フレーム順序のショートカットを防止する。

## 3. Scaling Analysis (スケーリング分析)
4つのモデルサイズ（59M〜743Mパラメータ）と3つのデータスケール（2.7M動画の1%・10%・100%）で評価。MSE はクリーンな冪乗則スケーリングに従い（R²=0.997）、NVSを「うまく振る舞うスケーリング問題（well-behaved scaling problem）」に変換した。静的シーンデータセット混合は同規模の一般動画データに劣り、スケーリングに必要なのはデータ量であることを実証。

## 4. Results (結果)
RE10KでPSNR 30.23・LPIPS 0.142を達成。ポーズ転移性（pose transfer R@10°: 0.92）でRayZer（0.48）を大幅に上回りXFactorと同等。6つのベンチマーク（LLFF・DTU・CO3D等）でのゼロショット評価で、RayDer-L（743M）は大規模video diffusion前学習を用いたSEVA（1.3B）と競合する性能を実現しながら、訓練計算量は1桁少ない。

## 5. Conclusion (結論)
RayDerは自己教師ありNVSが「うまく振る舞うスケーリング問題」であることを実証し、マルチネットワーク設計の複雑性と静的シーン制約を排除することで大規模動画からの効率的な学習を可能にした。未観測領域は低周波「平均推定値」となる制限はあるが、生成的ハルシネーションが発生しない安全な特性を持つ。
