## 1. Introduction (はじめに)
大規模ニューラルネットワーク学習においてパイプライン並列処理は不可欠だが、同期的手法はバブル（遊休時間）が発生し、非同期手法は重みバージョンのズレ（重み不整合）が生じるトレードオフが存在する。本論文は PACI（Pipeline Asynchronous training with Controlled Inconsistency）を提案。「重み保存なし・予測なし・グローバル同期なし」で前後方向のバージョンドリフトを制限する非同期手法。

## 2. Related Work (関連研究)
同期的パイプライン並列処理（GPipe、PipeDream-Flush、Megatron-LM 等）は一貫性を維持するが、フラッシュやバリア同期によるバブルオーバーヘッドが問題。非同期手法は重み保存（PipeDream）、予測機構（XPipe、PipeOptim）、学習率リスケーリング（PipeMare）で不整合に対応するが、追加メモリ・計算・複雑性が増大する。PACI は勾配累積を局所的バージョン制御機構として活用し、既存手法より簡潔に不整合を制御する。

## 3. Method - PACI (手法詳細)
核心的観察：「非同期性単体ではなく、前後方向計算間の最適化更新回数が不整合を支配する」。各段階は前方計算直後に後方計算を実行するが、パラメータ更新を a 回の勾配累積ごとに遅延させる。ローカルな「準備中前方計算数」を制限する flow-control ルール（q_i < N+1-i）と組み合わせ、最大不整合を Δ_max ≤ ⌈(N-1)/a⌉ に上界化する。重み保存・予測不要で実現。

## 4. Experimental Setup (実験設定)
GPT-2 Medium を OpenWebText で学習。8GPU（RTX PRO 6000 Blackwell Max-Q）パイプライン構成、グローバルバッチサイズ 128 または 256、固定 49.8B トークン予算。BF16 精度、AdamW 最適化器。同期的 1F1B-flush との比較実験を複数ランダムシードで実施。

## 5. Results (実験結果)
安定性: Δ_max ≤ 2 の範囲で PACI は 1F1B-flush の損失軌跡に追従し、最終損失は同等かやや改善。Time-to-accuracy: バッチサイズ 128 で最大 1.69 倍、256 で 1.41 倍の高速化を達成。バブルを排除しながら品質を保持。大規模（28.3B パラメータ、32GPU）では ZB-2p 相当のスループットを追加メモリなしで達成見通し。

## 6. Conclusion (結論)
有界不整合の許容範囲内で非同期パイプライン効率を回復できることを実証。現在の評価は GPT-2 Medium・8 段階パイプラインに限定されるが、実装のシンプルさと理論的な不整合上界の保証は大規模モデル学習への応用を示唆する。グローバル勾配クリッピングとの統合等が今後の課題。
