# From 2D Grids to 1D Tokens 詳細翻訳

## 1. はじめに (Introduction)
従来の多モーダル画像融合では 2D 特徴グリッドを共有表現として採用してきたが、照度やコントラストなどの「グローバル基盤因子 (base factor)」は空間座標に紐付かない性質を持つため、2D グリッド上では多くの位置に分散・エンタングルされ、調整が困難だった。本論文では「冷凍された事前学習済みの 1D トークナイザー」を導入し、グローバル要因の制御と局所詳細の復元を分離する軽量ハイブリッド融合フレームワークを提案する。

## 2. 関連研究 (Related Work)
画像圧縮トークナイザーは「2D 空間密集型」と「1D 列形式」に分類される。VQ-VAE/VQGAN などの 2D 型は高い局所性バイアスを持つ一方、TiTok・FlexTok といった最近の 1D 型は極度の圧縮と「より局在的なグローバル因子制御」を実現している。多モーダル融合分野では CNN から Transformer へ進展があるが、「融合は依然として密集 2D マップ上で実行され、グローバル外観手がかりが局所詳細と混在したまま」という課題が残存していた。

## 3. 2D 共有表現の根本的問題
2D 特徴グリッドには二つの根本的問題がある。第一に「局所性バイアス」: 基盤因子は空間に依存しない画像レベル属性であるのに、2D グリッドでは全情報が空間位置ごとに分布するため、表現形式と意味構造にミスマッチが生じる。第二に「不安定な基盤モデリング」: 低次元グローバル因子を高次元空間場から推定することは統計的に不安定で、「多対一逆問題」として条件数が悪化する。

## 4. 提案手法 (Methodology)
3 つの主要コンポーネントから構成される。**(1) 1D トークン表現**: 冷凍された TiTok トークナイザー (K=32 トークン、各 12 次元) で各入力をエンコード。**(2) 選択的トークン編集 (STE)**: Gumbel-Softmax プロービングでグローバル外観に敏感な位置 (12、18) とチャネル (6,7,8) を同定し、オフセット学習で選択的に編集。**(3) トークン-マップ界面**: 32×32 から 256×256 への段階的アップサンプリング時に、元画像から抽出した多スケール詳細特徴を融合。2 段階学習戦略 (Stage I: 単一モーダル再構成安定化、Stage II: クロスモーダル融合) を採用。

## 5. 実験結果 (Experiments)
M³FD・RoadScene・TNO・Harvard データセットで既存 9 手法を比較。エントロピー (EN)・標準偏差 (SD)・構造類似度 (SSIM) など複数指標で最良または準最良性能を達成。下流タスクでは M³FD での物体検出 (mAP50:95) と FMB でのセマンティックセグメンテーション (mIoU) 両領域で最高値。効率面では Text-DiFuse と比較して「FLOPs とレイテンシを大幅削減しつつ SSIM 向上」を実現。

## 6. 結論 (Conclusion)
本研究は多モーダル画像融合の共有表現を根本から再設計した。密集 2D グリッドの代わりに、冷凍 1D トークナイザーを「グローバル外観制御の軽量インターフェース」として導入し、2D 経路による局所構造復元と組み合わせることで、「照度一貫性向上・詳細先鋭化・アーティファクト削減」を同時実現。複数ベンチマークでの一貫性向上と下流応用での実用性証明により、この新しい表現パラダイムの有効性を実証した。
