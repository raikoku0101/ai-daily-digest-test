## 1. はじめに (Introduction)
LLMエージェントは複数ステップ調整と外部リソース活用のためスキル(能力パッケージ)に依存するが、現在のシステムでは「SKILL.md」形式の自然言語でスキルが表現されており機械処理が困難。本論文はスキル情報を構造化形式に変換する「Scheduling-Structural-Logical (SSL) 表現」を提案。スキル呼び出しインターフェース・実行フェーズ構造・原子的アクションと資源利用の証拠を3層に分離する。

## 2. 関連研究 (Related Work)
LLMエージェント研究は単一ツール呼び出しから複雑なスキルリポジトリへと進化。言語知識表現の古典理論(Schankら)からインスピレーションを得て、スキルを目標指向コンテキスト・順序付けアクティビティ・プリミティブ操作として分解できる。スキル検索とリスク評価はテキストのみでは実装困難な重要課題。

## 3. 手法 (Method)
SSLの3層階層構造:

### Scheduling層 (スキルインターフェース)
スキルレベルの呼び出しシグネチャ (invocation signature) と依存関係 (dependency) を記録。他スキルとの連携に必要なインターフェース情報を構造化。

### Structural層 (シーングラフ)
実行フェーズをシーングラフ (scene graph) として表現。「準備→取得→推論→実行→検証→復旧」等の段階を明示的に構造化。

### Logical層 (ロジックステップグラフ)
「読取・選択・比較・推論・書込」等の原子的操作 (atomic operation) と資源スコープ (resource scope) を定義するロジックステップグラフ (logic-step graph)。

LLMベースの正規化器 (normalizer) が既存SKILLファイルをこの構造に自動変換。

## 4. 評価 (Evaluation)

### スキル発見タスク (Skill Discovery)
6,184件のスキル候補から正しいスキルを検索する評価。SSL活用手法でMRR (Mean Reciprocal Rank) が0.573→0.707に向上 (+23.4%)。

### リスク評価タスク (Risk Assessment)
500件のスキルを「データ流出・破壊的行動・権限昇格」等6次元で評価。SSL含む場合、マクロF1が0.744→0.787に改善 (+5.8%)。

## 5. 議論と結論 (Discussion & Conclusion)
SSLは元の自然言語文書と「証拠インターフェース (evidence interface)」として併用する設計。構造化表現は検索と安全性審査を改善するが完全置換ではない。スキル構成 (skill composition) や実行時利用にも拡張可能。スキル管理の実用的向上を示す重要な一歩。
