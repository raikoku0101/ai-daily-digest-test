## 1. Introduction (はじめに)

医療画像レポート自動生成、特に胸部 X 線レポート生成は臨床業務効率化の重要課題。従来の強化学習（RL）報酬設計は「正確性のみ」や「段階的プロセス」に基づくが、放射線レポートの複数知見は「順序に依存しない集合（order-agnostic set）」として存在する。本研究はこの構造的特性に対応する「集合間距離（set-distance）報酬」を提案する。

## 2. Related Work (関連研究)

医療 VLM（Vision-Language Model）は多くの臨床タスクに適用されており、最近の RL 微調整手法（GRPO 等）が推論能力を向上させている。既存の報酬設計は二値的正確性やプロセス報酬に焦点を当てるが、連続的な報酬による改善も検討されている。放射線レポートの非順序性という医学的特性に着目する点が本研究の独自性である。

## 3. Method (手法)

**レポート表現**: 各レポートを文単位に分割し、frozen sentence transformer（all-mpnet-base-v2）で埋め込みベクトルの集合 ℰ(y) に変換。

**報酬設計**: 形式報酬（テンプレート妥当性）と意味報酬（集合距離）を組み合わせる。意味報酬は Chamfer 距離（近傍平均）と Hausdorff 距離（最悪ケース）の 2 種類を採用し GRPO 学習に利用。

**推論時選択**: テスト時に K 個の候補を生成し訓練分布（訓練レポート埋め込み）に最も近い候補を選択。早期刈り込み機構により生成途中で低スコア候補の生成を停止し 50%以上のトークン削減を実現。

## 4. Experiments (実験)

MIMIC-CXR と ReXGradient で評価。Qwen3-VL-2B/4B・Gemma3-4B の 3 モデルで検証。Chamfer 報酬（ReXGradient）または Hausdorff 報酬（MIMIC-CXR）による微調整が SFT と正確性ベース報酬の両者を上回り、平均で BERTScore +6.80%・RadGraph F1 +7.82%・CheXbert F1 +4.45% の相対改善を達成。

## 5. Results: Inference-time Selection (推論時選択)

13 モデル（自社 GRPO 版 + Mistral・Gemini・GPT-4o 等の閉鎖系 LLM）で評価。集合距離ベース選択によりランダム選択比で平均 16.4% 改善。刈り込み機構で 50% 以上のトークン削減を達成しつつ品質を維持した。

## 6. Conclusion (結論)

集合距離報酬は訓練時 RL 微調整・推論時最良候補選択・効率化の 3 場面で有効な統一的シグナルとなる。胸部 X 線レポート生成において非順序的知見集合の特性を適切に捉える設計により、複数の VLM および閉鎖系 LLM の性能向上を実現した。
