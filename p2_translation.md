## 1. はじめに (Introduction)

密集検索 (Dense Retrieval) は高次元埋め込み空間を通じて文書をランク付けするため、その意思決定プロセスが不透明です。既存の説明手法は語彙的マッチングやトークン対応など表面的な信号に依存しており、埋め込みレベルでの潜在要因の洞察が限定的でした。本論文は Xetrieval という埋め込みレベルの機械的説明 (Mechanistic Explanation) フレームワークを提案し、クエリ文書ペアの関連性を疎 (Sparse) で人間が解釈可能な特徴に分解します。

## 2. 手法 (Method)

Xetrieval は「推論内部化器 (Reasoning Internalizer)」と「機械的説明器 (Mechanistic Explainer)」の 2 つの主要コンポーネントから構成されます。推論内部化器は、軽量な MLP を用いて「要約」「目的」「質問応答」の 3 つの推論側面を LLM 生成 CoT (Chain-of-Thought) から直接埋め込み空間に近似します。機械的説明器はスパース自己符号化器 (Sparse AutoEncoder: SAE) を採用し、推論強化埋め込みを解釈可能な疎特徴に分解します。複数のドキュメント側ビュー間で特徴オーバーラップを集約し、個別の検索決定を説明します。

## 3. 実験 (Experiments)

7 つの検索ベンチマーク (BRIGHT、NQ、MuTual 等) と 8 つの密集検索器で評価。TopK-SAE (k=256) が再構成誤差と単語義性のバランスが最適であることを確認。推論内部化器は基本検索器と比較して 2〜5% の NDCG@10 向上を達成し、CoT 推論器の性能の一部を保持しながら計算効率を確保しています。

## 4. 結果と分析 (Results & Analysis)

機械的説明器により生成された疎特徴は一貫性があり人間が解釈可能。提案手法は基本 SAE より大幅に優れた検出スコアを達成。推論内部化器が埋め込み空間をより分解しやすくし、豊かな特徴レベル要因を回復します。局所帰属と課題レベルステアリング実験で、Xetrieval の選定特徴の介入効果が直接分解より強いことを確認。大規模コーパスでの説明時間は CoT 推論器より数桁高速です。
