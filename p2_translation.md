# CanvasAgent 章別日本語要約

## 1. Introduction (はじめに)
複雑な画像生成・編集タスクは、単一のモデル呼び出しでは対応できません。ユーザーリクエストには、シーン生成、オブジェクト定位、領域分割、選別コンテンツ編集、中間資産合成、テキスト認識、解像度向上などが含まれます。これらのタスクは「長期的（後続操作が前段階の出力に依存）」「視覚グラウンド（各ツール呼び出し後に中間結果を検査）」「状態管理（複数画像・マスク・切り抜きが共存）」という特性を持ちます。既存のマルチモーダルツール使用エージェントは知覚最適化が主で、複数ツール間の実行可能な軌跡の大規模監督が不足しています。

## 2. Related Work (関連研究)
ツール拡張マルチモーダルエージェントは、ReActフレームワークに基づき、言語モデルを視覚財団モデルと連携させます。近年のMLLM（マルチモーダル大言語モデル）は視覚検査・実行推論を向上させていますが、主に認識・検索・推論対象です。画像編集システム（InstructPix2Pixなど）は個別操作に特化し、マルチステップ依存や状態管理が限定的です。ツール統合政策学習には、PPOやGRPO（グループ相対政策最適化）などの強化学習が活用されています。

## 3. Method (手法)
CanvasCraft データセット構築：11の視覚ツール（生成、編集、定位、分割、抽出、合成、変形、OCR、超解像）を統一インターフェースで実装。CanvasCraft-SFT（14万個の完全注釈付き実行軌跡）は「ツール連鎖設計→画像サンプリング→逆向き命令生成→実行」で構築。CanvasCraft-RL（1万個のRL用タスク仕様）は推論複雑度・軌跡長・ツール多様性で分類し、難易度設定します。二段階訓練フレームワーク：SFT段階で完全軌跡からツール呼び出しフォーマットと依存関係を学習。GRPO段階で「ハイブリッド報酬（結果スコア0.4＋過程スコア0.6）」を用いて弱監督下での探索を最適化。

## 4. Experiments (実験)
250サンプルの評価セット、Qwen3-VL、Qwen-Image-2.0など複数基準モデルとの比較。CanvasAgent(SFT)は基準モデルから報酬0.426→0.557へ向上。CanvasAgent(SFT+RL)は0.821達成、整合性0.869、美的0.762、軌跡スコア0.849。ツール呼び出し数は1.32→5.44へ増加。アブレーション研究は結果＋過程報酬の相補性を確認。

## 5. Conclusion (結論)
CanvasAgentは複雑な画像生成・編集向けの初のツール統合マルチモーダルエージェントです。140Kの軌跡と10Kのタスク仕様でスーパービジョンを提供し、SFT+GRPO訓練で最終画像品質と実行可能性を両立。自動評価・人間評価で効果確認されました。今後は動的ツール発見、学習型報酬、効率的ロールアウト戦略への拡張が課題です。
