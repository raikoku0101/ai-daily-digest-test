## 1. はじめに (Introduction)

3D Gaussian Splatting（3DGS）は高品質なリアルタイム3Dシーン表現として注目されているが、既存のフィードフォワード推論手法はピクセルアラインまたはボクセルアラインのアプローチに依存し、大量の冗長プリミティブを生成してしまう。入力ビュー数が増えると表現サイズが線形に肥大化し、グローバルな一貫性も損なわれるという根本的な問題があった。

## 2. 問題定義 (Problem Definition)

既存フィードフォワード手法の主要課題は以下の3点である。(1) ピクセル/ボクセルアラインによる冗長プリミティブ生成、(2) ビュー数増加に伴う表現肥大化、(3) クロスビュー対応解決前のデコードによるグローバル一貫性の欠如。これらはすべて「局所的でヒューリスティックな割り当て戦略がグローバルなシーン認識を欠く」という共通の根本原因に帰着する。

## 3. GlobalSplat 提案手法 (Method)

「align first, decode later（先にアライン、後にデコード）」原則に基づくフレームワークを提案。コンパクトなグローバル潜在シーン表現を学習し、マルチビュー入力をエンコードしてクロスビュー対応を解決してから、明示的な3Dジオメトリ（Gaussians）をデコードする。事前学習済みピクセル予測バックボーンや密なベースラインの潜在特徴を再利用せず、粗→細の段階的訓練カリキュラムで表現の肥大化（representation bloat）を防止。

## 4. 実験 (Experiments)

RealEstate10KとACIDの2つの室内シーンベンチマークで評価。比較対象は密なピクセルアラインフィードフォワード手法および反復最適化手法。評価指標はPSNR・SSIM・LPIPSによるNovel View Synthesis品質と、推論速度・表現サイズ。

## 5. 結果 (Results)

わずか16K Gaussiansという少数プリミティブで競争力のあるNovel View Synthesis性能を達成。モデルフットプリントは4MBと軽量で、単一フォワードパスで78ms以下の推論速度を実現。密なパイプラインと比較して表現サイズを大幅に削減しつつ、同等以上の画質を維持。

## 6. 結論 (Conclusion)

グローバルシーントークンによるフィードフォワード3DGSが、表現の圧縮性・再構成品質・推論速度のトレードオフを同時に解決できることを実証した。事前学習バックボーン不要の軽量設計により、AR/VR・自律ロボティクス・リアルタイムNVSアプリケーションへの実用展開が加速することが期待される。
