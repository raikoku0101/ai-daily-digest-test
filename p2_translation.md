## 1. Introduction (はじめに)

現代の大規模言語モデルは128K〜1M以上のコンテキスト長を扱うようになった。しかし、標準的なスケール内積注意（Scaled Dot-Product Attention, SDPA）は「二次的な計算量とメモリ使用量」をもたらす。本研究はLighthouse Attentionを提案する——階層的な選択機構を備えた訓練専用の注意アルゴリズムで、既存のFlashAttentionを活用しながら長コンテキスト訓練を加速できる。重要な特徴は、訓練後に完全注意モデルへ復帰可能な点である。

## 2. Related Works (関連研究)

注意機構の効率化は二つの主流に分かれる。圧縮・枝刈り手法（線形注意、SSM、MoBA、DeepSeek Sparse Attention等）はカスタムカーネルを必要とし、長距離recall能力が低下する場合がある。既存の階層的手法（NSA、HISAなど）は「キーとバリューのみ圧縮しクエリは密のまま」とする。Lighthouseはこれらと異なり、Q/K/V対称池化、選択とAttentionの分離、パラメータ不要スコアリングを特徴とする。

## 3. Method (手法)

Lighthouse Attentionは4段階パイプラインで動作する。

**ピラミッド構築**: Q, K, Vを対称的に平均池化（Average Pooling）し、L層のピラミッドを構成。各層ℓで池化係数pℓにより圧縮された三つ組（Q(ℓ), K(ℓ), V(ℓ)）を生成。

**スコアリングと選択**: ℓ2ノルムベースのパラメータ不要スコアで各ピラミッド要素を評価し、チャンク分割ビトニックカーネル（Bitonic Top-k Kernel）で上位k要素を抽出。この操作は微分不可能で勾配は流さない。

**密部分列注意**: 選択された要素をS≪N長の連続部分列に集約し、標準のFlashAttentionで処理。因果性はピラミッド座標から自動的に保証される。

**散布復帰（Scatter-back）**: 出力をpℓ個の基本位置に分散し元のN長を復元。計算量はO(N·d)に削減される。

## 4. Experiments & Results (実験と結果)

530Mパラメータ Llama-3スタイルモデル、C4データセット、98,304トークン/シーケンス、16,000オプティマイザステップ（約50.3Bトークン）で評価。

主要結果：
- 512Kコンテキストで順伝播21倍、順+逆伝播17.3倍の高速化
- 2段階訓練（Lighthouse→SDPA復帰）で全体密訓練ベースラインより低い最終損失を達成
- 総経過時間1.4〜1.7倍短縮、スループット84〜126k tok/s/GPU（密SDPA比約2倍）

## 5. Conclusion (結論)

Lighthouse Attentionは対称Q/K/V池化・選択とAttentionの分離・パラメータ不要スコアリングを統合し、長文脈事前訓練を効率化する。学習専用アルゴリズムとして訓練後にSDPAに完全復帰できる点が核心的な利点。今後は適応的予算選択、マルチモーダル拡張が課題。
