## 1. Introduction (はじめに)

「大規模言語モデル（LLM）の多様性」に焦点を当てた分散品質多様性探索フレームワーク DEI を提案。従来の並列進化探索は単一モデルを複数ワーカーで複製するため、そのモデルの帰納的バイアスが全体に反映される。本研究では異なる LLM（GPT-5.4-mini・Claude Sonnet 4.6 等）を異なるノードに割り当て、各モデルの創造的性質を相補的な行動新規性の源として活用する。「非同期チャンピオン共有」により同期バリアなしに異質ハードウェア環境での協調を実現する。

## 2. Background (背景)

Core War（メモリ上で Redcode プログラムが戦闘する競技的プログラミング）を対象とする。MAP-Elites（品質多様性アルゴリズムの標準）は「行動特性空間（behavior space）」を離散化し各セルに最高適応度の解を保持する。Digital Red Queen（DRQ）は各ラウンドのチャンピオンを敵として機能させ進化的軍拡競争を生み出す。異質ノード間では遅延差が大きいため同期バリアを避ける「非ブロッキング集合通信」が必要。

## 3. Method (手法)

各ノードは「非同期メッセージング層」と「ローカル DRQ オプティマイザ」を統合する。各ラウンドで T 回の LLM 呼び出し（10%は新規生成・90%は既存プログラムの変異）を実行。ノード間でチャンピオンを公開し他ノードのチャンピオンを敵対集合に追加することで「クロスモデル対抗圧力（cross-model adversarial pressure）」を生み出す。評価指標は「汎化性・アーカイブ被覆率（archive coverage）・QD-Score・ニッチ新規性」の 4 軸。

## 4. Results (結果)

等しい総 LLM 呼び出し予算下で、異質アンサンブル（4 ノード×62 反復≈250 呼び出し）は単一ノード比で QD-Score が 124% 高く（45.90 対 20.46）、被覆率が 28% 高い（80.6% 対 63.0%）を達成。同一モデル 4 ノード（均質アンサンブル）より多様アンサンブルが優れており、モデル多様性そのものが利得の主因であることを実証する。

## 5. Discussion (考察)

多様性アンサンブル > 均質アンサンブル > 単一ノードの階層が個別ノードの汎化性とマージアーカイブで一貫して成立する。非同期設計により遅いローカルモデルの追加は高速ノードにペナルティを与えず被覆率を改善する。ただし結論は Core War に限定され他ドメインへの一般化は未確認。

## 6. Conclusion (結論)

DEI は異質 LLM アンサンブルの異なる帰納的バイアスを行動多様性の主要源として活用し、非同期ゴシップベースの通信でクロスモデル対抗圧力を実現する。「並列化」ではなく「モデル多様性（model diversity）」を分散 LLM ベース探索の第一設計原理とすべきことを示唆する。
