## 1. Abstract (概要)

本研究は、ヒューマノイドロボットと環境・物体との相互作用をモデル化する課題に取り組みます。ExoActor フレームワークは大規模ビデオ生成モデルの汎化能力を活用し、「第三者視点からのビデオ生成（Exocentric Video Generation）を統一的インターフェースとして使用」することで、ロボット・環境・物体間の協調的相互作用（Coordinated Interaction）を暗黙的に符号化します。生成されたビデオから人体動作（Human Motion）を推定し、汎用動作コントローラー（General Motion Controller）で実行可能な行動列に変換します。

## 2. Introduction (はじめに)

ヒューマノイドロボット制御システムは進展していますが、「空間的文脈（Spatial Context）、時間的ダイナミクス（Temporal Dynamics）、ロボット行動、タスク意図を規模において共に捉える」ことが根本的課題です。従来の教師あり学習は相互作用に富んだ行動（Interaction-Rich Behavior）のモデリングに不適切です。ビデオ生成を制御インターフェースとして活用することで、大規模ビデオデータに含まれるインタラクション知識を間接的に活用できるという革新的アプローチを提案しています。

## 3. Method (手法)

ExoActor パイプラインは 3 段階で構成されます。① タスク指示とシーン文脈から第三者視点ビデオを合成する生成モジュール（大規模ビデオ生成モデルを使用）。② 生成ビデオから人体動作を推定するポーズ推定ステップ（Motion Estimation）。③ 推定された動作をロボット実行可能な命令に変換する汎用動作コントローラー（General Motion Controller）。この設計により、暗黙的相互作用ダイナミクス（Implicit Interaction Dynamics）が自動的に捉えられ、タスク条件付き行動列（Task-Conditioned Behavior Sequence）を生成します。

## 4. Experiments & Results (実験・結果)

エンドツーエンドシステムとして実装され、新規シナリオへの汎化能力が検証されました。追加の実世界データ収集なしに複数の相互作用タスクで機能することが示されています。ビデオ生成モデルの大規模学習データに含まれるインタラクションパターンが自然に転移することが確認されています。

## 5. Conclusion (結論)

ExoActor は相互作用に富んだヒューマノイド行動モデリングに対するスケーラブルなアプローチを提供します。生成モデル（Generative Models）が汎用ヒューマノイド知能（General-Purpose Humanoid Intelligence）の発展を促進する新たな道筋を示唆。現在の実装の限界（ビデオ生成の物理的整合性・実ロボット転移誤差など）と将来研究の方向性も議論されています。
