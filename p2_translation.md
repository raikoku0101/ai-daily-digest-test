## 1. Introduction (はじめに)
コーディングエージェントは進化するリポジトリに対し字句検索・意味検索・シンボル操作・依存関係追跡を反復的に行うが、分散したインデックスと言語サーバーにより同じ情報の再発見が繰り返される。CodeNibは「リポジトリをデータベースとして扱う」アプローチを採用し、コミット単位の不変基本データから複数の派生ビューを構築・管理する体系を実現。

## 2. System Overview (システム概要)
「コミット→ビルド→ランタイム」の三段階構成。
①Repository View Compiler: コード塊・セマンティックバックエンド・ビルダーを統合し、字句・密ベクトル・構造化グラフの3ビューを別々に構築してManifestに登録。
②Query Planning and Retrieval: ランク付きテキスト検索とシンボルナビゲーションをルーティング・再ランキング・グラフ拡張に低レベル化。
③Agent Runtime: 既存ビューを読み込み、検索・参照・定義ツールを公開し、プロンプト履歴への文脈配信ポリシーを適用。

## 3. Repository Views (ビューの詳細)
各エンティティは「ファイル(L₀)・型スコープ(L₁)・呼び出し可能定義(L₂)」の三段階で表現。
- Lexical View (字句ビュー): 識別子・パス・コメントのポスティング記録 (BM25/Zoekt)
- Dense View (密ベクトルビュー): 単位のベクトル埋め込み (FAISS Flat/IVF/HNSW)
- Structural View (構造ビュー): 包含・依存・参照関係を有向グラフで表現 (igraph)
各ビューが独立して更新されるため、一方の失敗が他のビューの利用可能性を損なわない。

## 4. Incremental Updates (増分更新)
Git差分とLSPによる「ファイルレベル削除・再接続」と「シンボルレベル分類・修復」の2戦略を検証。シンボルレベル修復は削除・影響受・シフト・不変・追加にシンボルを分類し位置をリベースするためLSPリクエスト数を削減。全更新出力は独立再構築との offline 比較で検証: 中央値8.67倍 (グラフ)・25.44倍 (ベクトル) の高速化を達成。

## 5. Evaluation (評価)
Q1: Dense+Rerank vs. Dense+Graph Fusion vs. Structural のトレードオフを100スナップショットで測定。
Q2: L₀/L₂ × 5言語 × Flat/IVF/HNSW の構築時間・検索レイテンシ・品質の相互関係を分析。
Q3: 静的グラフ提供者と5言語のlive LSPを1,000リクエストで比較。
Q4: 8リポジトリ33グラフ・31ベクトル遷移で増分更新を検証。
Q5: 5モデル×2,500クエリで文脈配信ポリシーを評価: Eager+Compactで50〜87%のトークン削減 (精度低下 ΔAR@5≥-5% 以内)。

## 6. Conclusion (結論)
CodeNibはリポジトリコンテキストを「単回タスク探索」から「再利用可能なマルチビュー」へ転換。コミット・ソースアドレスという共有基盤の上に字句・密・構造的ビューを独立構築・更新・提供し、各操作の有効性境界と生成コストを明示的に記録。ステージ分離Pareto分析により品質・互換性・更新忠実性・レイテンシ・トークン使用量を多次元評価。
