## 1. Introduction (はじめに)
Video LLM（大規模言語モデル）は優れた動画理解性能を示すが、大量の視覚トークン処理による推論遅延が実用展開の障壁。既存圧縮手法はエンコーダ後期段階にのみ着目し、ビジョンエンコーダ自体の効率化は未着手。TTFTプロファイリングでエンコーディングがTTFTの大半を占めることを実証し、エンコーダ内での早期圧縮という新アプローチを提案。

## 2. Related Work (関連研究)
既存圧縮手法はエンコーダ内圧縮（ToMe、LLaVAPruMerge）、LLM前段圧縮（DyCoke、HoliTom）、LLM内圧縮の三分類。これらは後期段階圧縮に集中しビジョンエンコーダの計算量削減に未対応。「注意シンク現象」（特定空間位置が過度に高い注意スコアを獲得）がTop-K選択に偏りを生じさせることも指摘。

## 3. Method (手法)
EarlyTomは二段階戦略で構成。**第一段階**：ストリーミング方式でフレーム類似度に基づき動的セグメント化し、類似フレームを加重融合で統合するエンコーダ内フレーム圧縮。**第二段階**：フレームを動的フレーム（セグメント首尾）と静的フレーム（中間）に分類し、それぞれ全体Top-K vs. 局所ウィンドウ選択を適用する分離型空間トークン選択（Decoupled Spatial Token Selection）。

## 4. Experiments (実験)
LLaVA-OneVision-7B/0.5Bで評価。MVBench・EgoSchema・LongVideoBench・VideoMMEの4ベンチマーク使用。10%トークン保持率でTTFT 2.65倍削減・FLOPs 61%削減を達成しながら、フルトークンベースライン比で96%以上の精度を維持。スループット32.9トークン/秒で最高値を記録。

## 5. Main Results (主要結果)
FLOPs比44.2%（36.5T）、TTFT 426.3msという最高加速度を既存手法中で達成。LLaVA-Video・Qwen2.5-VLなど異なるアーキテクチャや0.5B〜7Bのスケール差でも安定した効率改善と精度維持を確認。実世界の大規模ビデオ処理への即時適用可能性が高い。

## 6. Conclusion (結論)
ビジョンエンコーダ内での早期段階圧縮という新しいパラダイムを確立。トレーニング不要という実用性と、TTFT・FLOPs・精度の三つを同時改善する効率性が特長。Video-LLMの商用展開コストを大幅削減する実践的貢献として、動画AI産業全般への波及効果が期待される。
