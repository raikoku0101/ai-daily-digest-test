## 1. Introduction (はじめに)

マルチモーダル LLM の進歩が GUI エージェントへの関心を高める一方、実世界の多様なアプリケーションにわたる大規模学習データの不足がエージェントの汎化を制約している。既存データセットは高コストな手動アノテーションに依存し、特定ドメインに限定される。ラベルなしインターネット動画という豊富なリソースを活用した自動的なデータ収集フレームワークが必要とされていた。

## 2. Related Work (関連研究)

GUI エージェントの事前学習データ構築に関する先行研究（OS-Genesis, VideoAgentTrek 等）は手動アノテーションや限定的な自動化に留まっていた。Video2GUI は完全自動化パイプラインによって規模と多様性の双方を大幅に拡張する点で差別化される。

## 3. Video2GUI Pipeline (提案手法)

**粗→細フィルタリング戦略（Coarse-to-Fine Filtering）**: 5億件のビデオメタデータから高品質な GUI チュートリアル動画を段階的に識別。粗フィルタリングでカテゴリ・タグベースの絞り込みを行い、細フィルタリングで GUI 操作を実際に含む動画かどうかを検証する。

**軌跡変換**: 選別された動画を構造化されたエージェント軌跡に変換。スクリーン録画と低レベルインタラクションログから GUI アクション（クリック・入力・スクロール等）の時系列を抽出し、グラウンディング情報（座標・要素）と紐付ける。

## 4. WildGUI Dataset (データセット)

1500以上のアプリ・ウェブサイトをカバーする1200万件のインタラクション軌跡で構成。Windows・macOS・ウェブブラウザなど多様なプラットフォームのプロフェッショナルから一般ユーザー向けまで幅広いソフトウェアを網羅。アノテーションコストをゼロに近づけながら既存データセットの数百倍の規模を実現。

## 5. Experiments (実験評価)

Qwen2.5-VL と Mimo-VL を WildGUI で事前学習し、複数の GUI グラウンディング・アクション実行ベンチマーク（ScreenSpot, GUI-Odyssey 等）で評価。WildGUI による事前学習で一貫した 5〜20% の改善を達成し、最先端性能に並ぶまたは上回る結果を示した。

## 6. Conclusion (結論)

Video2GUI は GUI エージェント訓練のデータ問題をラベルなし動画の活用で実用的に解決した。WildGUI データセットとパイプラインのオープン公開により、GUI エージェント研究コミュニティ全体の底上げが期待される。RPA・業務自動化・アクセシビリティ支援への応用が加速する見通し。
