## 1. Introduction (はじめに)
本論文は、LLM エージェントが過去の経験から学習した「スキル」を実行可能なプログラム関数（Program Functions, PFs）として表現する HASP フレームワークを提案しています。従来のテキストベースのスキルは助言的に機能するのみですが、本手法により、いつどのように介入するかを明示的に制御できる実行可能なスキルが実現されます。

## 2. Related Work (関連研究)
既存研究では Reflexion、ExpeL、Voyager など複数のスキル拡張型エージェントが提案されていますが、ほとんどのアプローチではスキルが「テキスト形式の受動的な指導」のままです。HASP はこれらと異なり、スキルを「状態-行動介入関数（State-Action Intervention Function）」として実装し、ランタイムで直接制御可能にします。

## 3. Method (手法)
HASP は 3 つのモジュール成分から構成されます。(1)推論時 PF 介入：ベースポリシーの提案行動に対し、外部ハーネスが `should_activate` と `intervene` メソッドを持つ PF を評価・実行し、行動修正またはコンテキスト注入を行います。(2)ポストトレーニング（Post-Training）：各 PF 実行から「介入のタイミング・方式・正確性・効果」を記録し、構造化信号でスコアリング後、棄却サンプリング（Rejection Sampling）・SFT・on-policy 蒸留でモデル更新します。(3)自己改善：残存失敗から PF 候補を生成し、実行可能性検証と教師レビューによりノイズの少ないスキルライブラリへ進化させます。

## 4. Experiments (実験)
ウェブ検索、数学推論、コーディング 3 領域で評価されました。推論時 PF のみで平均精度が 51.0% に、教師支援で 56.2% に向上。ポストトレーニング（棄却サンプリング）で 59.3%、on-policy 蒸留で 62.5% に達成。閉ループ進化を組み合わせた主要バリアント（HASP-Evolve + RS）はウェブ検索で 60.3%、数学で 45.4%、コーディングで 69.9% の pass@1 を達成しました。

## 5. Conclusion (結論)
本研究は、エージェント経験を「状態-行動介入関数」として実行可能なスキルに変換する枠組みを提案しました。PF は推論時の介入、ポストトレーニングの監督信号、検証済みメモリの進化という 3 つの役割を果たし、モデルが既知の戦略をより確実に活用できるようにします。
