## 1. Introduction (はじめに)

テスト時スケーリング（test-time scaling）はLLMの推論能力を向上させる強力なアプローチだが、長時間生成ではKVキャッシュ（KV-cache）のメモリ増加が制約になる。既存のKVキャッシュ量子化手法はプリフィル設定での評価が主であり、自己回帰デコーディング（autoregressive decoding）時のエラー挙動を十分に考慮していない。本研究はタイムステップ間でのエラー蓄積メカニズムを分析し、トークンスケール誤差が主因であることを示す。

## 2. Preliminaries (基礎知識)

KVキャッシュ量子化は通常、チャネル次元ごと・トークンブロックごとにタイル処理される。KIVI手法ではK行列をチャネルごと、V行列をトークンごとに量子化する非対称アプローチが採用。Hadamard変換などの不整合処理（incoherence processing）はチャネル空間の外れ値（outlier）を均等化するが、トークン単位のスケーリング誤差には不十分。最大5%の外れ値エラーが全体のKLダイバージェンス劣化の大部分を引き起こすという重要な観察が提示される。

## 3. Methods (手法)

KVarNは3段階の処理を適用する。第1段階でチャネル次元のHadamard回転を実施。第2段階でチャネルとトークン両軸の反復的分散正規化（Sinkhorn様式）を適用。第3段階でRound-to-Nearest量子化を実行。エラー分解では「量子化エラー = (‖K‖-‖Kdq‖)²（大きさ誤差）+ 2‖K‖‖Kdq‖(1-cosθ)（方向誤差）」という形で分析し、外れ値は主に大きさの誤差に起因することを示す。提案の分散正規化はトークン単位の大きさを直接補正しエラー蓄積を軽減。

## 4. Experiments (実験)

Qwen3-4B、Llama-3.1-8B、Phi-4-14B上でMATH500、AIME24、HumanEval、IFEval、Line Retrievalタスクを評価。KVarNは2.3ビット/要素という低精度で全ベンチマーク最高性能を実現。AIME24ではKIVIの55.5%に対し60.0%の精度を達成。エラー蓄積を考慮した疑似デコード設定（pseudo-decode setting）で従来手法との差が顕著。分散正規化処理の計算オーバーヘッドはわずか0.18%と実用的。

## 5. Conclusion (結論)

KVキャッシュ量子化におけるエラー蓄積は不正なトークンスケーリングによる外れ値が主因。KVarNはHadamard回転と分散正規化の組み合わせでこの問題を解決し、テスト時スケーリングシナリオで最先端のKVキャッシュ圧縮を実現。実装はvLLMで利用可能であり即時活用可能。
