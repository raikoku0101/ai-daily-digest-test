## 1. Introduction（導入）
推論能力を持つLLMの後学習において、CoTデータを含まない「no-CoTデータ」（教科書の導出や問題解答など）をいかに活用するかが課題。既存研究では強化学習による「次チャンク推論（Next-Chunk Reasoning）」が提案されていたが、本研究は「Mixed SFT」という単純な代替案を提案し、根本的な比較検証を実施。

## 2. Experimental Setup（実験設定）
Qwen3-30B-A3B-Baseをベースモデルとして使用。「長CoTデータ」(152K例、約1.95Bトークン)と「no-CoTデータ」(421K例、約0.53Bトークン)を比較。比較対象: (1)次トークン推論(NTR)、(2)次文推論(NSR)、(3)順序型SFT、(4)混合型SFT、(5)推論専用SFT。全手法は同じRLVR(検証可能報酬による強化学習)段階を経て評価。

## 3. Main Results（主要結果）
Mixed SFTはRLVR前の精度が最も低い(27.5%)にもかかわらず、RLVR後の最終性能が最も高い(61.1%)。数学競技問題(AIME、HMMT、IMO)で平均67.4%、域外推論タスク(HLE、GPQA、MMLU-Pro)でも一貫して最高性能。次チャンク推論RLは60倍以上の追加計算を要しながらMixed SFTに及ばない。

## 4. Analysis（分析）
次チャンク推論の失敗メカニズム: 「高エントロピートークンは実際には局所的に予測可能」「生成される推論トレースはテンプレート的な局所補完に退化」。Mixed SFTの事前学習時の性能低下は「形式の不一致」による一時的現象で、RLVR段階で修復されることを実証。

## 5. Related Work（関連研究）
DeepSeek-R1、Qwen3など混合データでのSFT効果が示唆されていたが、「長CoTとno-CoTを単一段階で合わせたベースラインと比較した研究がない」という重要なギャップを本論文が指摘・解消。

## 6. Discussion & Conclusion（結論）
「計算予算が同じ場合、Mixed SFTは次チャンク推論RLより単純で効果的」。高い事前学習精度が最終性能に直結しないという方法論的洞察も重要。SFT段階内のデータ構成最適化が追加RL段階より有効。
