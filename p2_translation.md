## 1. Introduction (はじめに)
産業規模のユーザー表現学習では、ユーザー数・行動履歴長・モデルサイズを増やすことで性能が向上すると考えられていた。しかし本研究は、Alipayの数億ユーザーデータを用いた大規模実験で「生の行動データをそのまま拡大しても、ある閾値を超えると性能向上が鈍化する」という問題を発見。この現象を「生行動データのスケーリング壁」と定義し、根本原因は「データ量ではなく情報密度」にあると主張する。提案解決策は、長く冗長な生行動系列をタスク関連の信号を保持する密集したトークン表現に変換する「行動密化（behavioral densing）」である。

## 2. Related Work (関連研究)
既存研究は三つの方向に分類される。第一に生行動データのスケーリング研究は、生行動系列長を段階的に拡大することの限界を指摘するが、密度改善の観点は欠落。第二にトークン化手法（VQ-VAE、RQ-VAE等）は離散表現構築に注力するが、スケール依存の配置戦略は未検討。第三に適応的量子化研究は入力レベルの割当を扱うが、最適容量設定への一般的指針を提供していない。本研究は「生データ・トークン化・容量設定・適応割当」の四側面を統合する初めての作業である。

## 3. Scaling Law Analysis (スケーリング壁の分析)
**ユーザー数軸**：N=0.03B以上では性能向上が飽和。新規ユーザーは主に冗長な行動パターンをもたらす。**時間軸**：D=60日超過時、追加の履歴情報の限界効用がほぼゼロに。観測期間を60日から120日に延長しても改善は限定的。**モデル容量軸**：0.2B→0.4Bパラメータ拡大時、損失は低下するが精度はほぼ不変。これは情報密度が根本的制約であることを示唆する。

## 4. Densing Gains from Tokenization (トークン化による密化効果)
RQ-VAE（残差量子化）を用いて長い行動系列を固定長の離散トークンに圧縮。同等の計算予算下で、生系列と比較してトークン化系列が飽和領域（D≧64日またはN≧10^7ユーザー）で優位性を発揮。冗長性抑制と情報集約が可能であることが実証された。

## 5. The Behavioral Densing Law (行動密化則)
情報密度を最大化する最適表現はパレート効率的なトレードオフ問題として定式化。最小充分なトークン容量C*はスケールsに対して対数スケーリング関係を示す。この関係が複数トークン化手法・複数データソース・複数下流タスクで一貫性を示すことを検証。勾配αはデータの内部多様性と量子化方式の表現空間冗長性に依存する。

## 6. New Method: ALGN (適応型可変長トークン化)
「固定長トークン化は行動の異質性を無視」という課題を解決するため、適応型長ゲーティングネットワーク（ALGN: Adaptive Length Gating Network）を提案。各量子化段階で残差ノルム（説明されていない情報量）と符号化不確実性から継続確率を計算し、限界効用が限界コストを下回ったら打ち切り。複雑な行動セグメントは深いコードを、単調な習慣は浅いコードで表現。固定RQ-VAEと比較して容量63%削減、AUC/KS/精度で1〜2%向上。

## 7. Conclusion (結論)
本研究は「より多くのデータとより大きなモデルが常に最適」という前提を初めて大規模実験で反論した。スケーリング壁の発見と、情報密度重視という新たな設計原理を提示。将来への示唆は「データ蒐集量」から「信号抽出効率」への問い立てシフトである。
