# KVpop: Key-Value Cache Compression with Predictive Online Pruning — 詳細解説

## 1. Introduction (はじめに)

大規模言語モデルの自動回帰デコーディングではKV（Key-Value）キャッシュが線形に増加し、長文脈推論の大きなボトルネックとなる。本論文が提案するKVpopは「トークンの将来の有用性を予測して不要なトークンを削除する学習型キャッシュ圧縮手法」。既存手法と異なり、削除判定を保護ウィンドウの境界で実施し、その時点で蓄積された文脈情報を活用できる。訓練時に将来注意（future attention）を監督信号として使う新しい学習パラダイムを実現している。

## 2. Related Work (関連研究)

KVキャッシュ削減手法は三つのカテゴリに分類される。第一に「疎な検索（Sparse Retrieval）」は全キャッシュを保持しながら各ステップで関連トークンのみを選択。第二に「ヒューリスティック削除」は固定ルール（注意スコア等）で恒久的にトークンを削除。第三に「学習型削除」は削除ポリシーを訓練時に習得する。DMC（Dynamic Memory Compression）やDMS（Dynamic Memory Sparsification）が既存の学習型手法だが、削除予定トークンがウィンドウ内に滞在中に蓄積される証拠の活用が不十分だった。

## 3. Method (手法)

KVpopの核心は三つの要素で構成される。

**将来注意監督信号（Future Attention Supervision）**: 各トークンが保護ウィンドウを出た後に受け取る平均注意確率を目標値として、スコア予測器の訓練に使用する。密な注意行列を具体化せずに効率的に計算可能。

**転置注意による効率的な目標計算（Transposed Attention）**: クエリとキーの役割を交換する転置注意パスを実装。S×S密行列を回避しながら、スパース注意カーネルからのLSE（log-sum-exp）正規化項を再利用して正確な監督信号を得る。

**遅延スコアリング（Deferred Scoring）**: mLSTM（多重ゲート線形RNN）ベースのメモリ型スコアラーが、トークン挿入時ではなく削除判定境界で評価。保護ウィンドウ内での近未来文脈を活用したより精密な判定を実現する。

## 4. Experiments (実験)

Qwen3-4BおよびQwen3-8BにKVキャッシュ圧縮率75%/88%でAIME（数学競技）、HMMT数学競技問題での評価を実施。圧縮率75%でQwen3-4Bの密注意性能の95%、圧縮率88%で94%を保持。既存手法DMS比で8〜16ポイントの相対性能優位を達成した。GPQA Diamond、LiveCodeBenchへの転移性能でも堅牢な結果を示す。

## 5. Conclusion (結論)

KVpopは「削除判定時点での将来注意教師信号」と「遅延メモリ型スコアリング」の二つの革新により、長文脈推論品質を大幅に損なわずにKVキャッシュを積極的に圧縮することを可能にした。数学推論タスク訓練済みモデルがコード生成やSTEM推論ベンチマークへも転移性を示すことから、提案手法の汎用性が確認された。
