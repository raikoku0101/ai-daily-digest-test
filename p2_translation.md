## 1. Introduction (はじめに)
デプロイ済み長時間稼働型AIエージェントは「エージェント劣化（agent aging）」という新しい障害モードに直面している。モデルの重みが固定されていても、メモリ圧縮（memory compression）、履歴蓄積、状態更新、メンテナンス操作によってエージェントの有効状態が変化し続ける。本研究は「エージェント寿命工学（Agent Lifespan Engineering: ALE）」という新しい評価パラダイムを提案し、デプロイ後の信頼性低下がどのように発生し、どこで修復すべきかを診断するフレームワークを構築する。

## 2. Agent Aging Taxonomy (エージェント劣化の分類体系)
劣化メカニズムを4種類に整理する。**圧縮劣化（Compression Aging）**: 書き込み時点で未来の情報ニーズが不明なため、低頻度の詳細（金額・固有名詞）が喪失される。**干渉劣化（Interference Aging）**: 蓄積された類似エントリが検索時に目的事実を埋没させる。**改訂劣化（Revision Aging）**: 事実変更時に更新が正しく伝播されず、派生状態に複合誤差が蓄積する。**保守劣化（Maintenance Aging）**: メモリ再圧縮やログ削除などの定期メンテナンス後に急激な性能低下が発生する。

## 3. AgingBench Framework (ベンチマーク構成)
**時間的依存DAG（Temporal Dependency DAG）**構造でマルチセッション関係を符号化。バージョンチェーン（事実の上書き関係）、依存エッジ（マルチセッション合成タスク）、干渉ペア、蓄積器を包含。各シナリオで依存密度・更新率・鎖深度・干渉密度を独立制御可能なプログラマティック生成器を実装。

## 4. Component-Level Attribution (成分別故障診断)
メモリパイプラインを明示的に分解: **Write (W)** → 圧縮劣化の診断地点、**Retrieval (R)** → 干渉劣化、**Utilization (U)** → 改訂劣化、**Store/Lifecycle (S)** → 保守劣化。反事実的プローブ（counterfactual probes）として3段階介入条件（P1:ベースライン → P2:検索oracle化 → P3:文脈gold化）を設計し、各段階間の精度差から故障箇所を特定。

## 5. Results (実験結果)
単一モデルが全メカニズムで支配的ではなく、劣化タイプはモデルにより異なる。行動準拠性と事実精度は独立的に劣化し、「確信を持った不正確な回答」は行動テストでは検出困難。改訂劣化はモデル規模増大で必ずしも改善しない。ワークスペース忠実度が下流想起精度を常に上回り、故障はWriteではなくUtilization段階に位置する。

## 6. Conclusion (結論)
長時間稼働型エージェントはモデル重みが凍結されていても、メモリ状態ドリフトとメンテナンスを通じて静かに劣化する。AgingBenchは劣化の4分類と時間的依存構造・成分別診断で「何が破損し、なぜ破損し、どこから修復するか」を答える。劣化は一次元ではなく、行動テストで隠蔽可能で、モデル内で構造的に急激であることが示された。
