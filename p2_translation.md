## 1. Introduction (はじめに)
Looped Transformerは層を反復実行して有効深度を増加させる手法だが、従来評価は計算量増加と建築的利点が混在。本研究はトークンあたりFLOPs・総パラメータ数・KVキャッシュを厳密一致させてMoE（Mixture-of-Experts）Transformerのループを研究。中間50%を2回ループするSMELTレシピで検証損失において6.8〜18.0%の訓練FLOPs削減を実現。

## 2. Design Recipe (設計レシピ)
3つのアブレーション実験により設計方針を確立：(1) 全層でなく中間50%のみループ、(2) ループ型モデルは大きな有効深度幅比率を好む、(3) 2回ループが最適（3〜4回は過剰）。MoEとLoop Transformerを組み合わせることで、計算コストをパラメータ節約で補償する設計を実現。

## 3. Scaling Laws (スケーリング則)
100M〜1.6B非埋め込みパラメータの4スケールで訓練。Chinchilla形式のスケーリング則をSMELTと基準モデルに別々にフィット。SMELTの損失が計算量に対してより急速に低下することを確認。スパース性が高いほど削減率が増大。計算最適境界C=10²¹ FLOPsで14.7%削減。

## 4. Downstream Performance (下流タスク性能)
DCLM Coreで96対中83対に勝利。DCLM Completionでは全96対で勝利。検証損失の改善から予測される以上の下流性能向上を観測（超過利益はモデル規模と共に増大）。コードドメインで最大20.4%の利益。長いサンプルや多くの文脈内例がある場合に利益が増幅される傾向。

## 5. Conclusion (結論)
計算量を厳密一致させた条件でもループはTransformerを改善できる。特に構造化データと文脈内学習（In-Context Learning）で強い効果を発揮するSMELTは、深度再利用を測定可能な利益に変える実践的な手法。
