## 1. Introduction (はじめに)

テスト時間スケーリングにおける LLM の多様な応答生成の課題に対処。標準的な確率的サンプリングは「表層的な字句変動のみ」を生じさせ、意味的な多様性に欠ける。Exploratory Sampling (ESamp) は、ニューラルネットワークが過去に遭遇した入力には予測誤差が低く、未知の入力には誤差が高いという性質を活用。軽量な潜在蒸留器 (Latent Distiller) をテスト時にオンライン学習させ、LLM の浅層から深層への隠れ表現写像をモデル化。

## 2. Related Work (関連研究)

既存の多様性促進手法は 3 分類。①Top-p・Min-p などの確率分布制約手法は計算効率的だが字句レベルの多様性のみ。②Tree of Thoughts・Diverse Beam Search などの構造化探索は解の質は高いが計算コストが膨大。③Contrastive Decoding・OverRIDE などのロジットレベル制御手法はボキャブラリー空間で冗長性を抑制するが、意味的に等価な異なる表現を区別できない。ESamp は表現空間で冗長性を推定する点で差別化。

## 3. Problem Formulation (問題設定)

LLM の生成を K 個の並列軌跡を持つマルコフ決定過程 (MDP) として定式化。目的は KL 正則化で基盤モデルからの乖離を制約しながら「意味的に未探索の領域への報酬 r(s,z)」を最大化すること。閉形式解: π*(z|s) ∝ π_ref(z|s)exp(r(s,z)/α)。

## 4. Methodology (手法)

**潜在蒸留器 (Latent Distiller)**: 2 層 MLP が LLM の第 1 層隠れ状態から最終層隠れ状態への写像を予測。予測誤差がセマンティック・ノベルティを示唆。

**ノベルティ駆動生成**: 真の深層表現と蒸留器予測表現の両者を言語モデル化ヘッドで射影し、ログ尤度比 r(s,z) = log π_ref(z|s) - log q_dist(z|s) を報酬とする。新しいサンプリング分布: π_new(z|s) ∝ π_ref(z|s)^(1+β) / q_dist(z|s)^β。

**協調的探索**: 蒸留器をバッチ全体で共有オンライン学習させることで並列生成間の暗黙的な「先着順スケジューラ」機能を実現。一つの軌跡が意味領域を探索すると、蒸留器がその写像を学習し他の軌跡の再訪を自動的に抑制。

**非同期実装**: 蒸留器の計算を LLM 中間層実行時に GPU 非同期ストリームで並列化。最悪時 5% 未満のオーバーヘッドを達成。

## 5. Experiments (実験)

AIME 2024/2025 (数学)、GPQA-Diamond (科学)、LiveCodeBench v5 (コード生成)、BookCorpus (創作文章) で評価。比較手法: vanilla 温度サンプリング、Min-p、FIRE、Tree of Thoughts、Contrastive Decoding、OverRIDE。評価指標: Pass@k、埋め込み類似度、Vendi Score (多様性)、困惑度 (一貫性)。

## 6. Results (結果)

推理タスクで特に顕著な改善。GPT-OSS-20B では Pass@64 相当の性能を Pass@8 で達成し、サンプリング予算を大幅削減。Pass@1 では単一サンプルの精度を維持または向上。創作文章では多様性と一貫性のトレードオフを打破。並列生成の軌跡間類似度は生成長を通じて継続的に低下し、蒸留器による協調的な意味領域の分散を実証。K=16 での並列化時でも吞吐量低下は 4.25% にとどまる。

## 7. Conclusion (結論)

表現空間のノベルティ信号を活用することで、効果的な意味的多様性を実現する実用的な手法。非同期パイプライン設計により無視できるレイテンシ増加で達成。複数のモデルファミリーと課題で堅牢な汎化性能を示す。コードは公開済みで、テスト時間スケーリングの効率向上に貢献。
