## 1. Introduction（はじめに）

現在のGraph Retrieval-Augmented Generation (GraphRAG) システムは、知識グラフ構築において単一パスの抽出を行うため、ノイズが多く脆弱です。本論文は3つの障害を特定します。第一に、単一パス抽出は情報の統合メカニズムを欠いています。第二に、従来システムは高価なLLM（GPT-4クラス）に依存していますが、パイプライン内で必要な能力は言語スキルであり、世界知識ではないという仮説を提示しています。第三に、多くのオープンソース実装は本番環境への対応が不十分です。著者らは「言語スキルは大規模モデルでも弱い成長を示すが、世界知識は急峻に拡大する」と主張し、コンパクトな特化モデルの活用を提案しています。

## 2. System Description（システム説明）

RAGUパイプラインは6段階の処理を実行します。段階1では複数の戦略（固定サイズ、セマンティック、スマートセマンティック）でチャンキングを行います。段階2では「二段階抽出（two-stage extraction）」を採用し、エンティティ抽出とリレーション抽出を分離することで、NERELスキーマ（29エンティティタイプ、49リレーションタイプ）に基づいた検証を実現します。段階3の統合ではDBSCAN + LLM要約により重複排除を行い、ノイズを低減します。段階4〜6では、Leidenクラスタリングでコミュニティ検出を実施し、構造化レポート生成と洗練化を行います。またMeno-Lite-0.1という7Bパラメータの特化モデルを開発し、単GPU環境での実行を実現しています。

## 3. Evaluation（評価）

GraphRAG-Bench（医学領域）、BioASQ、MuSiQue、2WikiMultiHopQAの4つのベンチマークで評価しました。すべてのシステムが同一の回答生成LLM（gpt-4o-mini）を使用し、グラフ構築品質を分離測定します。事実検索タスクではHippoRAG 2が優位ですが、複雑推論や文脈要約ではRAGUとの差が縮小します。特に「エビデンス想起（evidence recall）」では「RAGUが全ファクトイドレベルで最高の結果」（0.84 vs ≤0.76）を達成しました。Meno-Lite-0.1は「Qwen2.5-32B比で調和平均+12.5%」のIEベンチマーク性能を示します。

## 4. Demonstration（実証例）

Dennis Ritchieに関する文から、システムが9個のNEREL型エンティティ（PERSON、PRODUCT、ORGANIZATION、DATE等）と8個のリレーション（WORKS_AS、DATE_OF_DEATH、PARENT_OF等）を抽出します。Leidenクラスタリングにより2つのコミュニティに分割され、Ritchieの専門的遺産とBell Labs地理的クラスタが識別されます。マルチホップ検索（multi-hop retrieval）により「C言語クリエイターの父はどこで働いたか」という多段階質問に正確に回答できることが示されます。

## 5. Conclusion（結論）

RAGパイプライン内のLLMは世界知識ではなく言語スキルを必要とし、これらのスキルはモデルサイズとの相関が弱いという仮説を検証しました。RAGUは広範な文脈合成が必要なタスク（要約、創造的生成）で優位であり、チェーン追跡型システムは単一事実精度で優位です。両アーティファクトはオープンソースライセンス下でリリースされ、単GPU環境での実行を実現しています。
