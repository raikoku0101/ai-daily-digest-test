## 1. Introduction (はじめに)

3D 視覚学習では、既存のスキャン・メッシュ・点群から完全なシーン被覆を保ちながら冗長性を最小化したパノラマ RGB-D データを生成する方法が求められています。従来の密な軌跡は近接ビューの重複が多く、疎なヒューリスティックは重要領域を見落とし、ソース固有のレンダリング方針で注釈が不均質になる問題がありました。本研究はこの観測層を明示的・検証可能にすることを目指します。

## 2. Related Work (関連研究)

360度深度推定や NeRF 再構成などでパノラマデータの価値は認識されていますが、既存リソースは統一性に欠けます。本研究はオンライン次善視点計画ではなく、オフライン・訓練不要な固定予算キュレーションを実現し、選択プロセスの再現性と監査性を優先します。

## 3. Method (手法)

固定予算 K 下での Max-k-Coverage 問題（NP 困難）を貪欲法で近似します。核となる競合認識ワーピングオラクル（Warping Oracle）が低解像度パスで、蓄積点群を候補視点の ERP 枠に投影し、新規領域 N_v・既説明領域 E_v・競合領域 C_v を分類。スコア s_t(v) = G_t(v) - λL_t(v) で順位付けします（λ=0.35）。

補題1（競合認識ノイズオラクル）により、プロキシ誤差 ε が競合比で増幅される場合でも (1-1/e) 近似を保証。3段階パイプライン：(1)アセット正規化、(2)候補生成と26方向幾何フィルタ、(3)予算付き貪欲選択。

## 4. CM-EVS Dataset (データセット仕様)

全フレームを統一スキーマで提供：RGB（2048×1024）、メトリック深度（float32 メートル）、キャリブレーション済みポーズ。13 統一カテゴリーで全室種をカバー（Shannon entropy 3.10 ビット）。Blender 屋内 374 シーン（13,631 フレーム）、HM3D 401 室、ScanNet++ 500 スキャン、屋外 TartanGround/OB3D も含む計 36,373+ フレームを公開。

## 5. Analysis & Conclusion (分析と結論)

固定予算 K=4 での比較でランダム・単一視点プローブ・被覆のみ・低競合のみを上回る性能。λ∈[0.1, 0.5] で安定動作し、3 つのキュレーターソース全体に一貫した動作を確認。パノラマ観測構築を再現可能・監査可能な明示的プロセスとして定義し、幾何的一貫性と効率性を兼ね備えた観測層を提供。
