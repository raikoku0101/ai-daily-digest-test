## 1. Introduction（はじめに）
深層ニューラルネットワーク（DNN）はわずか数個のパラメータビット反転で壊滅的な性能低下を招く根本的脆弱性を持つ。攻撃者がモデルパラメータへの書き込みアクセスを得た場合、訓練データもモデル実行機会もなく少数のビット反転で深刻な障害を誘発できる。従来の敵対的攻撃（入力操作）や物理的攻撃と異なり、計算負荷が小さく検出困難である。

## 2. Problem Setup（問題設定）
攻撃者は訓練データアクセスなし・モデル実行機会なしという極めて制限的条件下でIEEE 754形式の浮動小数点パラメータのビットを直接操作。符号ビット（最上位ビット）の反転によるパラメータ値の劇的変化を活用。Rowhammer攻撃やファームウェア脆弱性など現実的なハードウェア/ソフトウェア手段を脅威モデルに含む。

## 3. Method: Deep Neural Lesion (DNL)（手法）
ゼロパス版DNL: パラメータの絶対値|θᵢ|に基づいて高い値を持つサインビットを優先的に反転。重要度刈り込み（pruning）文献の知見と初期層のパラメータが下流全体に影響するという理論的根拠に基づく。
1パス版1P-DNL: ランダム入力1つの単一前向き・逆向きパスを使用し、ハイブリッド重要度スコア = α|θᵢ| + β|勾配項 + ヘッシアン項| を計算。古典的「最適脳損傷（Optimal Brain Damage）」基準の敵対的アナログ。
層選択: 初期層（最初の10層）を対象とした攻撃が最終層より遙かに破壊的。神経生物学的直感と一致。
1カーネル1反転制約: 複数反転が相互に打ち消す現象を防ぐため単一カーネル内の反転数を1に制限。

## 4. Experiments（実験）
ImageNet画像分類: ResNet-50で1P-DNLによりわずか1ビット反転で精度99.4%低下。48モデル中43モデルが10反転で60%以上の精度低下。
推論LLM: Qwen3-30B-A3B（Mixture-of-Experts）で2ビット反転により78%から0%への完全崩壊。Qwen3-4B・Nemotron Nanoでも同様。
物体検出: Mask R-CNN/ResNet-50で2反転でCOCO検出AP・マスクAPが事実上ゼロに。YOLOv8-segでは1反転で77%以上のmetric低下。

## 5. Comparison（既存手法との比較）
BFA（Rakin et al. 2019）は勾配ベース反復探索とデータが必須。ZeBRA（Park et al. 2021）は合成データ生成を通じた最適化が必要。DNLは単一パスまたはゼロパスで同等以上の性能低下を達成。

## 6. Defenses（防御）
DNLで同定した臨界パラメータ（全体の1%）のみをビット複製やハミングコードで保護すれば、BFA攻撃を完全無効化可能。脆弱パラメータの正確な特定と防御コスト削減を両立。

## 7. Conclusion（結論）
DNNの隠れた脆弱性を暴露し軽量攻撃による実用的脅威を示唆。パラメータが部分的にのみアクセス可能な環境では効果が低下する可能性があり、耐性アーキテクチャや数値表現の今後の研究を促す。
