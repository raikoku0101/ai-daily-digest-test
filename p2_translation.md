## 1. Introduction (はじめに)

現代のLLMは128K、1M以上のコンテキスト長を必要としていますが、標準的なスケール付き内積注意（Scaled Dot-Product Attention: SDPA）の二乗計算量とメモリがボトルネックとなっています。本論文は訓練中のみ活用する選択ベースの階層的注意「Lighthouse Attention」を提案。訓練終盤で密な注意（SDPA）に戻すことで、推論対応モデルを得ます。

## 2. Related Works (関連研究)

既存アプローチは圧縮・枝刈り手法（線形注意、状態空間モデル）とブロック/トークンレベルの疎注意に大別されます。Lighthouseの決定的な違いは「対称的なQ/K/Vプーリング」と「選択をカーネル外部に配置」する点。NSA・DSA・HISAなどは「キー・バリュー側のみ圧縮し選択を注意カーネル内に組込む」のに対し、本手法は全三者を階層的に圧縮し、標準的なFlashAttentionを呼び出します。

## 3. Method (手法)

Lighthouse は4段階パイプラインで構成されます：

**(1) ピラミッド構築**: Q,K,Vをプーリング因子 p で L レベルの対称的ピラミッドに平均プーリング。各レベルはコヒーレントな三つ組を形成。

**(2) スコアリングとtop-k選択**: パラメータフリーな ℓ₂ ノルムに基づいて各ピラミッド要素を採点し、chunked-bitonicカーネルでtop-kエントリを選出。

**(3) 密部分列注意（Dense Subsequence Attention）**: 選択されたエントリを連続テンソルに集約し、標準的なFlashAttentionで処理。因果性は階層座標から自動的に保証。

**(4) スキャッター再構成**: 出力を「シフト窓」を通じて元のNトークン位置に分散。

## 4. Complexity Analysis (計算複雑度)

L=log_p(N/k)を選ぶと、1層あたりの計算量は Θ(T·d)（線形）。密SDPAの Θ(T²d) より大幅削減。選択がカーネル外部にあるため標準FlashAttention-3による最適化をそのまま享受でき、forward/backwardが密Transformerと「ビット単位で一致」します。

## 5. Experiments (実験)

530Mパラメータ（Llama-3様式）、C4データセット、98,304トークン長、16,000ステップで評価。

主な結果：SDPA回復後のモデルが完全密SDPA基準（損失0.7237）を上回る損失0.6980を達成。N=512Kでforward 21倍・forward+backward 17.3倍の高速化。長文脈検索（NIAH）でも基準（0.72）を上回る0.76の平均検索率。終端間訓練時間は22.5h（基準37.9h）と1.4〜1.7倍の加速。

## 6. Conclusion (結論)

Lighthouse Attentionは対称的Q/K/Vプーリングと選択のカーネル外部配置により、訓練効率と推論対応性の両立を実現。制限事項として、自己回帰デコーディングには非対応で推論時は密SDPA再開が必須です。
